# 2026-05-12 일지 — W19 후반 057 Phase 2.1e 운영 deploy + 위젯 미반영 fix

## 한 줄 요약

점심에 057 Phase 2.1e Step D-2 + E 완료 (운영 deploy + 198 row sync). 저녁 위젯 검증에서 5/11 사이트 새 공고 (oder 67/68) 운영 위젯 미반영 발견 → `merge_jb.py` 의 `notice_order`/`notice_chk` drop 버그 추적 → 3 패치 + v1+운영 즉시 반영 → 위젯 정상화 PASS. 단편 fix 시 전체 파이프라인 영향 추적 정책 신설 (백로그 061/062/063).

---

## 점심 — 057 Phase 2.1e Step D-2 + E 완료

### Step D-2 진단 (ADMIN_KEY mismatch 해소)

| 항목 | 결과 |
|---|---|
| ADMIN_KEY 양쪽 일치 | SHA prefix 9a87bcc17d26 (43자) — PowerShell 환경변수 ↔ Render Shell `os.environ['ADMIN_KEY']` 일치 |
| Render Suspend → Resume | 완료 (어제 cache 잔존 가능성 제거) |
| Manual Deploy "Clear build cache" | 어제 시도, 효과 없음 |
| localhost POST | 200 OK (web process 정상) |
| 외부 PowerShell curl POST | **403** |

### 외부 호출 격리 — curl escape 문제로 판정

```
py -c "import os, requests; r = requests.post('https://job-bizgov-platform.onrender.com/api/sync', json={'key': os.environ['ADMIN_KEY'], 'source':'jbtp', 'rows':[]}, timeout=15); print(r.status_code, r.text)"
```

→ **STATUS 200** `{"ok":true,"source":"jbtp",...}` → 시나리오 A 확정 (Python requests OK, PowerShell curl escape 문제). 향후 동일 호출은 `py -c` requests 또는 `sync_to_render.py` 사용.

### Step E — 첫 sync 실행

| source | inserted | updated | errors | marked_synced |
|---|---:|---:|---:|---:|
| jbtp | 0 | 128 | 0 | 128 |
| jbexport | 2 | 68 | 0 | 70 |
| **합계** | **2** | **196** | **0** | **198** |

v1 로컬 flag 검증: jbexport 70/70, jbtp 128/128 모두 `synced_to_render=1` PASS.

운영 DB 분포 검증 (Render Shell):
- `('jbexport', 1544, 70)` MAX(notice_order)=1544
- `('jbtp', 0, 128)` MAX=0

---

## 저녁 — 위젯 검증 실패 + 진단

### 브라우저 확인

| 사이트 jbexport 최신 5건 | 운영 위젯 jbexport 상위 5건 |
|---|---|
| 68: 전북AI무역지원센터 (5/11) | 1. 미국 관세 설명회 (5/8, oder 66) |
| 67: 전북FTA통상진흥센터 1차 설명회 (5/11) | 2. FTA 5차 교육 (4/27, 65) |
| 66: 미국 관세 설명회 (5/8) | 3. 미주/유럽/중동 언택트 (4/24, 64) |
| 65: FTA 5차 교육 (4/27) | 4. 디지털 마케팅 (4/23, 63) |
| 64: 미주/유럽/중동 언택트 (4/24) | 5. 중국 시장 (4/16, 62) |

**문제**: 5/11 사이트 새 공고 oder 67, 68 가 운영 위젯 상위 5에 안 보임. 위젯이 oder 66 부터 시작.

### 자율 진단 흐름

1. **v1 jbexport 최근 7건 조회** → id=24225, 24226 가 5/11 신규, **notice_order=0** 확인 (둘 다 `synced_to_render=1`)
2. **시나리오 B 확정** (parser/merge 단계 손실)
3. **데이터 흐름 추적**:
   - 사이트 upstream API 직접 호출: `oder=1545, 1546` 정상 (SP_SEQ 매칭 검증)
   - `data/jbexport/2026-05-12.json` (today.json snapshot): `notice_order=1545, 1546` 정상
   - `data/all_jb/all_jb.json` 의 24225/24226: **`notice_order` 키 자체 누락** ❌
4. **근본 원인**: `pipeline/merge_jb.py` 의 정규화 함수가 return dict 에 `notice_order`/`notice_chk` 누락
5. **보조 원인**: `data/history/`, `data/merged/`, `data/filtered/`, `data/all_jb.json` (self) 같은 derivative 파일이 jbexport raw 보다 먼저 `seen_keys` dedup 캐시에 진입 → raw row dropped

---

## Fix — `pipeline/merge_jb.py` 3 패치

| # | 위치 | 변경 |
|---|---|---|
| 1 | `_normalize_item` line 214 return | `notice_order`, `notice_chk` 패스스루 추가 |
| 2 | `_normalize_jb_new_item` line 419 return | 동일 패스스루 |
| 3 | `merge_jb_json` dedup loop line 327 | 같은 key 만나면 기존 row 의 빈 `notice_order`/`notice_chk` 를 새 row 값으로 보강 (`seen_index` 도입) |

### 왜 update_db 의 merge 로직이 안전망 못 됐는가

`update_db.py:488` 의 백로그 049 로직 "새 값=0 이면 old 값 유지" 는 **기존 row 의 정렬키 보호** 의도. 신규 INSERT 의 첫 row 는 old=0 (DEFAULT) → 안전망 무용. derivative 파일이 dedup 우선순위를 가져가는 사고를 막지 못함.

---

## 적용 + 검증

1. `merge_jb` 재실행 → `all_jb.json` 의 24225/24226 에 `notice_order=1546/1545` 들어감 확인
2. `update_db` 실행 → v1 DB 갱신 (24225→1546, 24226→1545)
3. v1 DB `UPDATE biz_projects SET synced_to_render=0 WHERE id IN (24225, 24226)` (2 rows)
4. `sync_to_render --source jbexport` → `inserted=0, updated=2, errors=0`
5. v1 위젯 정렬 확인: top 2 = 1546, 1545 PASS
6. 운영 위젯 브라우저 검증: 사이트 oder 67/68 두 공고가 위젯 1·2위 노출 PASS

---

## 5/12 commit / push

| repo | commit | 설명 |
|---|---|---|
| v1 | `75d5265` | `fix(b057): jbexport notice_order merge 단계 drop 사고 해결` |
| v2 | `47ebdf3` | 동일 (v1 cherry-pick) |
| v1+v2 | (this) | `docs(b057): 5/12 daily + 백로그 061/062/063 + INDEX 갱신` |

---

## 진짜 교훈 — 단편 fix 의 추적 한계

1. **백로그 049/051 (jbexport 위젯 정렬 도입) 작업이 merge 파이프라인까지 추적 안 함** — 그 시점 `_normalize_item` 의 return dict 에 `notice_order` 추가 안 됐던 게 6일간 잠재.
2. **`update_db` merge 로직 "새 값=0 이면 old 유지" 안전망이 결함을 가렸음** — 049 backfill 로 기존 row 의 notice_order 가 채워져 있어서 신규 INSERT 만의 문제로 보였고, 그 신규 INSERT 가 5/11 까지 0건이었기에 잠재.
3. **5/11 신규 공고 등록 = 결함 노출** — 운빨로 발견. 신규 공고가 더 늦게 들어왔다면 더 오래 stale.
4. **다음부터 단편 fix 시 "전체 파이프라인 영향 추적" 정책 필수**.

→ 정책 후속 백로그 3건 신규 등록.

---

## 신규 백로그 등록

| ID | 제목 | 우선순위 | 비고 |
|---|---|---|---|
| 061 | 전체 파이프라인 명세 (사이트→fetch→merge→DB→sync→widget) | **HIGH** | 다음 백로그 작업 전 필수 |
| 062 | E2E 파이프라인 테스트 자동화 | **HIGH** | run_pipeline 끝 검증 hook |
| 063 | derivative cache 정책 (raw 우선순위) | MEDIUM | 본 사고 보조 원인 |

---

## 다음 액션

1. 백로그 061 명세 작성 — 058 / 059 진행 전 선행
2. 백로그 062 E2E 검증 hook 설계
3. 백로그 058 (at_global 진단) / 059 (AI 옵션 우선순위) 는 W20 으로 이월

---

## 미해결 / 이월

- 🟢 백로그 058 (v1 로컬 connector 정지 진단) — W19 마라톤 잔여
- 🟢 백로그 059 (AI 활용 강화 옵션 우선순위) — 사용자 결정 대기
- 🟢 백로그 057 Phase 2.4~2.6 (UPDATE merge 화이트리스트 / 실패 알림 / 통계 대시보드) — W20+
- 🟢 백로그 044 (DB_PATH) / 045 (v1/v2 분리) — 이월 누적

---

## 5/12 (저녁) 종료

- 057 Phase 2.1e Step D-2 + E **완료**
- jbexport 위젯 미반영 사고 **fix + 검증 PASS**
- 정책 후속 백로그 3건 등록

---

## 밤 추가 (22:00~) — jbtp 사이트 url 파라미터 변경 대응 + v1 누적 갱신 + Render sync

### 발견
저녁 jbexport fix 검증 후 사용자가 jbtp 위젯 추가 확인 — 사이트 5/11 신규 공고 oder 2201 (ILP 4차), 2200 (농식품 테크) 가 운영 위젯 미반영, 5/8 옛 공고만 표시.

### 진단 — 3중 원인 (Fix B' 진단 보고)

| # | 원인 | Evidence |
|---|---|---|
| 1 | jbtp connector 5/8 이후 미실행 | v1 DB `MAX(created_at)=2026-05-07 11:39:23`, 5/11 신규 row 부재. v1 어디서도 `connector_jbtp` import 안 함 (`__main__` 만) → 매번 수동 실행 |
| 2 | connector 에 notice_order/notice_chk/start_date 추출 로직 자체 부재 | `save_to_db` `want` list 미포함 → 128 row 전부 `notice_order=0`. 백로그 053 v2 fix 가 v1 미반영 (029 surgical 결정으로 보류) |
| 3 | 사이트가 2026-05 a[href] 파라미터 순서 변경 | 옛: `boardId&dataSid&menuCd`, 새: `menuCd&boardId&dataSid` — `idx_url UNIQUE` 우회 위험 |

진단 시나리오 A (oder 추출 못함) + 운영 5/11 신규 누락 = 두 별개 사안 동시. URL 일치 검증, `data/jbtp` 디렉토리 부재 (debug_save 호출 흔적 0) 로 미실행 사실 확정.

### 사고 #1 — 첫 connector 실행 시 url 변경 미인지 중복 INSERT

`py connectors/connector_jbtp.py` 1차 실행 → count 128→254 (옛 128 + 새 126), 117 dataSid 가 양쪽 row 존재. `INSERT OR IGNORE` 의 `idx_url UNIQUE` 가 string 매칭이라 같은 dataSid 도 파라미터 순서 다르면 별개로 봄.

→ 즉시 백업 복원 (`biz.db.backup_b057_jbtp_fixB_20260512_221131`, SHA256 사전·사후 매칭) → count 128 원복.

### Fix B' — connector url 정규화 (백로그 029 surgical)

`connectors/connector_jbtp.py` `+14줄`:

```python
_DATASID_RE = re.compile(r"dataSid=(\d+)")

def _normalize_detail_url(href: str) -> str:
    # 옛 row(boardId&dataSid&menuCd) 와 url string 매칭 위해 옛 형식으로 통일 (idx_url UNIQUE 호환).
    m = _DATASID_RE.search(href or "")
    if not m:
        return urljoin(BASE, href)
    return (
        f"{BASE}/board/view.jbtp?"
        f"boardId=BBS_0000006&dataSid={m.group(1)}&menuCd=DOM_000000102001000000"
    )

# parse_list_page: detail_url = urljoin(BASE, href) → _normalize_detail_url(href)
```

DRY 검증: page1 parse 46건, top5 dataSid 가 모두 NEW (5/11 신규 추정) — 옛 row 와 url 매칭 확인 후 본 실행.

### 재실행 + 검증

| 단계 | 결과 |
|---|---|
| connector 재실행 | count 128→137 (+9), 중복 dataSid **0** |
| 5/11 신규 INSERT 확인 | id=25084 'ILP 4차', id=25085 '농식품 테크' |
| `backfill_jbtp.py` apply | matched=126, updated=126, no_match=11 (옛 9페이지 외 deep row) |
| AFTER 분포 | `notice_chk=1`: 36, `start_date` 채워짐: 126, `MAX(notice_order)`=20248 |
| v1 위젯 시뮬 (공지 제외 top5) | 2203/2202/2201/2200/2199 = 사이트 캡처 **5/5 매칭** |

### 사고 #2 — backfill DRY_RUN 미적용

`$env:DRY_RUN="1"` PowerShell 문법이 Bash 툴 환경 (git bash) 에서 미주입 → backfill 이 곧장 apply 모드. 다만:
- 스크립트 자체 백업 (`biz.db.backup_20260512_221847_053_jbtp_widget_e169a748dc0c`) 생성
- 결과가 의도 최종 상태와 일치
- 멱등 — 재실행 시 unchanged=126

→ DB 손상 없음. 향후 PowerShell env 주입은 PowerShell 툴 사용 또는 `set` 분리 명령.

### Render sync — ADMIN_KEY 부재 (사고 #3) + 사용자 1회 주입

| 단계 | 결과 |
|---|---|
| synced_to_render reset | UPDATE rows=137 (옛 128 backfill 변경 + 새 9) |
| DRY_RUN | pending=137, sample payload 에 `notice_order`/`notice_chk`/`start_date` 정상 포함 |
| 1차 apply | HTTP 403 `admin key required` — 진단: `ADMIN_KEY` env len=0, `.env` 부재 → 스크립트가 default `dev-admin-key` 사용 중 (마스킹 `***...ey` 가 단서) |
| 사용자 ! 키 1회 주입 | `inserted=9, updated=128, errors=0, marked_synced=137` |
| v1 사후 | synced=1: 137, pending: 0, MAX(synced_at)=2026-05-12 13:25:34 |

### 사용자 검증 PASS

운영 위젯 top5 = 사이트 캡처 oder 2203/2202/2201/2200/2199 정확 매칭 (사용자 브라우저 확인 — "공고는 일치함").

### 보안 — ADMIN_KEY 회전 권장

본 대화에 키 평문 1회 노출 → 1M transcript 잔존. Render Dashboard 에서 회전 권장 (별도 작업).

### 잔존 / 다음

- **scheduler 부재**: v1 에서 `connector_jbtp` import 없음 → 다음 5/13 신규 row 들어오려면 사용자 PC 에서 수동 실행. cron/Win Task 등록은 별도 백로그
- **백로그 029 통째 sync 보류 유지**: `notice_order` 추출은 여전히 `backfill_jbtp.py` 의존 (`_normalize_detail_url` 만 surgical 추가). v1/v2 jbtp connector divergence 본질 미해소
- **ADMIN_KEY 회전**: 별도 작업

### 변경 파일

| 파일 | 변경 |
|---|---|
| `connectors/connector_jbtp.py` | `+14줄` (`_normalize_detail_url()` + `parse_list_page` 1줄 호출) |
| `docs/daily/2026-05-12.md` | 본 섹션 추가 |
| `docs/backlog/057_jbtp_sync_diagnosis.md` | Phase 2.1f follow-up 섹션 추가 |

### 5/12 commit / push (밤 추가분)

| repo | commit | 설명 |
|---|---|---|
| v1 | `0032f32` | `fix(b057): jbtp connector url 정규화 (사이트 파라미터 순서 변경 대응)` |
| v1 | `06c02fe` | `docs(b057): 5/12 밤 daily + 057 Phase 2.1f follow-up` |
| v1 | `e5f20a1` | `docs(b057): release/2026-05-12_b057_v2_cherrypick + INDEX 갱신` |
| v1 push | 완료 | `c25dd0b..e5f20a1  main -> main` |
| v2 cherry-pick (docs 0002) | 완료 | `f7ff1fd docs(b057): 5/12 밤 daily + 057 Phase 2.1f follow-up (jbtp 누적 갱신)` push 완료 |

---

## 한밤중 (00:00~) — 029 통째 sync (jbtp) + #1 dry-test 사고 → 백로그 064 신설

### 진행 합의
사용자 결정: `0001 코드 patch v2 포팅 (029 통째 sync) + #1 scheduler 등록 + 어드민 회전 권고 삭제`.

### #3 어드민 회전 권고 삭제
memory `feedback_admin_key_rotation.md` 저장 — 향후 보고에서 ADMIN_KEY 회전 권고 자동 제외.

### #2 029 통째 sync (jbtp 만) — 적용 + 검증

| 단계 | 결과 |
|---|---|
| v2 connector_jbtp.py + _common.py 본문 review | 표준 4단계 분리 (fetch/parse/normalize/save), 217줄. v2 는 url 정규화 미반영 → 통합 필요 |
| 신규 `connectors/_common.py` 추가 (v2 본문, 161줄) | make_session / debug_save / save_to_db / paginate |
| `connectors/connector_jbtp.py` 교체 (v2 + `_normalize_detail_url` 통합) | 표준 4단계 + url 정규화 (0001 의도) 동시 반영 |
| DB 백업 | `db/biz.db.backup_b029_sync_pre_20260512_225334` (SHA256: `e6152756a901e993`) |
| 실행 | 9페이지 paginate → 시도 126, 신규 0, 중복 126 (url UNIQUE 정상) |
| AFTER | count 137 유지, 중복 dataSid 0, MAX(notice_order)=20248, 위젯 5/5 매칭 유지, url 패턴 137건 전부 표준 |
| commit `fd79792` | `refactor(b029): jbtp connector v1/v2 통째 sync — 4단계 분리 + _common 도입` |

→ jbtp connector 의 backfill 의존 제거 (이젠 connector 자체가 모든 필드 추출).

### #1 scheduler 등록 dry-test — 사고 4건 발견 + 보류

`run_all.py` 에 `run_aux_crawlers()` 함수 추가 (jbtp/jbbi/at_global/kseafood/jbtp_related 5개 non-fatal 자동 호출) + `run_all()` 에서 호출. dry test (실제 5 connector 실행):

| connector | BEFORE | dry-run AFTER | 결함 |
|---|---|---|---|
| jbtp | 137 | 137 (+0) | ✅ url 정규화 적용됨 (fd79792) |
| at_global | 203 | 409 (+206) | 🚨 옛: `proj_detail_id=0&proj_id=8182` / 새: `proj_id=7937&proj_detail_id=0` |
| kseafood | 244 | 489 (+245) | 🚨 (Base64 idx 디코드 정규화 필요 추정) |
| jbbi | 369 | 741 (+372) | 🚨 (dataSid+menuUUID 추정) |
| jbtp_related | 71 | 141 (+70) | 🚨 (jbtp 와 같은 사이트, dataSid) |

**합계 +893 row 잠재 중복 INSERT**. 4 connector 모두 url 파라미터 순서 변경 결함.

→ 즉시 백업 복원 (SHA256 매칭) + `git checkout run_all.py` 로 변경 되돌림. DB 손상 0, code 변경 미적용.

### 본 사고의 의미

1. **#1 의 사전조건 = 4 connector url 정규화 fix** — 단순 자동 호출 추가는 자동 사고 유발
2. **dry-test 가 사고 사전 발견** — 백로그 062 (E2E hook) 가 본 사이클에서 가치 입증
3. **사이트 url 변경 시점 미상** — 5/12 jbtp 우연 발견. 다른 4 사이트는 언제부터 결함이었는지 모름 → 모니터링 정책 필요

### 신규 백로그 064 등록

`docs/backlog/064_connector_url_normalization.md`:
- Phase 1: 4 connector 의 url 패턴 분석
- Phase 2: 각 connector 에 `_normalize_detail_url()` 추가 (jbtp 패턴 일반화)
- Phase 3: dry-test 검증
- Phase 4: #1 scheduler 등록 재시도

### 5/12 한밤중 commit / push

| repo | commit | 설명 |
|---|---|---|
| v1 | `fd79792` | `refactor(b029): jbtp connector v1/v2 통째 sync — 4단계 분리 + _common 도입` |
| v1 | (this) | `docs(b029+b064): 5/12 한밤중 daily + 백로그 064 신설 + INDEX 갱신` |
| v1 push | 진행 | `e5f20a1..` |
| v2 cherry-pick | 보류 | 064 완료 후 진행 (코드 fd79792 는 v2 의 4단계 패턴과 동일 — direct merge 가능, 그러나 사이클 분리) |

### 미해결 / 이월

- 🟢 백로그 064 — 4 connector url 정규화 (#1 사전조건)
- 🟢 #1 scheduler 등록 — 064 완료 후 재시도
- 🟢 v2 cherry-pick — fd79792 (코드) 는 064 완료 후 묶어서 진행
