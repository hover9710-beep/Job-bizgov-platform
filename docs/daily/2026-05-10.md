# 2026-05-10 일지 — 백로그 047 + 048 (jbexport 표시 정확도)

## 한 줄 요약

5/9 발견된 "운영 사이트 jbexport 항목이 원본 jbexport.or.kr 과 거의 일치하지 않음" 진단 → **백로그 035 (5/8) 백필이 잘못된 selector 로 다른 사업의 title 을 덮어씌웠던 버그** 확정.
selector 교체(백로그 047) + status sync 누락 코드 fix(백로그 048) 동시 완성. v1 DB 백필 + Render SQL 산출.

---

## 진단 chain

### Phase A — 사이트 vs DB diff

`scripts/debug/jbexport_site_vs_db_diff.py` 작성. 사이트 `getWork1Search.do` 직접 POST → list raw 받아 DB 매칭.

| 항목 | 값 |
|---|---|
| 사이트 work_year=2026 list | 68건 |
| v1 DB jbexport | 66건 |
| 첫 50 사이트 row 매칭 | 매칭 48 / 미존재 2 / **status 불일치 다수** |

핵심 신호: 사이트 list 에서 "접수마감" 인 사업 다수가 DB 에서는 `status='진행'` 으로 stale.

### Phase B — selector 오작동 증명

`scripts/debug/jbexport_titleview_audit.py`: 7개 spSeq detail 페이지의 `span.titleView` 첫 매치 비교.

```
spSeq=9810ffef (예상: 미주/유럽/중동 언택트) → 잡힌 title = "FTA통상진흥센터 5차 교육"
spSeq=eb876dda (예상: FTA통상진흥센터 설명회) → "FTA통상진흥센터 5차 교육"
spSeq=4f3a1a39 (예상: 해외통상거점센터)       → "서울푸드 연계 상담회"
spSeq=611bf9a5 (예상: 수출애로 해소)         → "온라인전시관 콘텐츠 제작지원"
...
```

→ **모든 페이지에서 첫 `span.titleView` 가 그 사업의 title 이 아닌 인접 사업의 title**.

### Phase C — selector 위치 식별

`scripts/debug/jbexport_detail_html_dump.py`: HTML 구조 dump.

```
[span.titleView] count=2
  [0] parents=['dd', 'dl', 'li', 'ul.board_bottom', 'div.content']
  [1] parents=['dd', 'dl', 'li', 'ul.board_bottom', 'div.content']
```

→ **`ul.board_bottom` (이전/다음 게시물 navigation) 안의 인접 사업 title** 을 잡고 있었음.

진짜 본문 title 위치:
1. `<table>` 내부 `td.th="지원사업"` 옆 td (카테고리 prefix `[교육/컨설팅]` 등 포함)
2. 페이지 끝 `<script>$('title').text('... | 사업명')</script>`

### Phase D — 대안 selector 검증

`scripts/debug/jbexport_title_selector_test.py`: 7 spSeq × 2 selector.

| selector | 통과 |
|---|---|
| A) `td.th="지원사업"` 옆 td | 6/7 (1건은 whitespace normalize 차이뿐) |
| B) `<script>$('title').text(...)` regex | 7/7 |

A 채택 (카테고리 prefix 보존), 매칭 실패 시 B 로 fallback.

---

## 백로그 047 — selector 교체 + 백필

### 코드 수정 (pipeline/jbexport_daily.py:147-167)

```python
# before (백로그 035, 잘못된 selector)
title_tag = soup.select_one("span.titleView")

# after (백로그 047)
for tr in soup.select("table tr"):
    th_td = tr.select_one("td.th")
    if th_td and "지원사업" in th_td.get_text(strip=True):
        sib = th_td.find_next_sibling("td")
        if sib:
            for noise in sib.select("span.blue_txt, span.red_txt, ..."):
                noise.decompose()
            ...
            break
if not out["title"]:
    m = re.search(r"\$\('title'\)\.text\('([^']+)'\)", html)
    ...  # | split, 마지막 segment
```

### v1 DB 백필 4건

`scripts/debug/jbexport_b047_backfill.py`. 백업: `db/biz.db.backup_b047_pre_backfill_20260510_083439`.

| id | spSeq | BEFORE title | AFTER title |
|---|---|---|---|
| 10064 | 4f3a1a39 | [코트라] 서울푸드 연계 상담회 | [시장조사/지사화] [변경공고] 해외통상거점센터 |
| 10066 | 9fbace94 | 2026 JB 해외 바이어 | [기타 지원사업] (중동 긴급) 수출 물류비 |
| 12425 | 9810ffef | FTA통상진흥센터 5차 교육 | [온라인 마케팅] 미주/유럽/중동 언택트 |
| 20283 | eb876dda | FTA통상진흥센터 5차 교육 | [교육/컨설팅] 전북FTA통상진흥센터 설명회 |

start_date / end_date / period_text 도 사이트 진실로 재추출.

---

## 백로그 048 — status stale (동시 발견)

### 근본 원인

`pipeline/jbexport_daily.py:filter_open_announcements` (line 569-578) 가
`OPEN_STATUSES = {"접수중", "공고중"}` 외 항목을 today.json 에서 제거 →
update_db 흐름에 마감 사업이 도달하지 못함 → 기존 DB row 의 status 갱신 0.

`update_db._upsert_one` 자체는 status UPDATE 를 한다 — 단지 마감 row 가 그곳에 도달하지 못할 뿐.

### 코드 fix (pipeline/jbexport_daily.py:1062+)

신규 함수 `sync_status_to_db(all_items)`:
- url 정확 일치로 매칭
- `status` / `raw_status` 만 UPDATE
- 메일/알림 흐름 (today.json → update_db) 변경 없음

`run_daily()` 끝에 호출 추가.

검증: 단독 호출로 stale row 1건 → 마감 정정 OK.

### v1 데이터 정정 4건

| id | spSeq | BEFORE | AFTER | 사업명 |
|---|---|---|---|---|
| 10061 | 8260a0d4 | 진행/공고중 | **마감/접수마감** | 전주시 AI 마케팅 |
| 10062 | c02eb9e7 | 진행/접수중 | 마감/접수마감 | 중국 시장 테스트 마케팅 |
| 10063 | 1d902514 | 진행/접수중 | 마감/접수마감 | 서울푸드 상담회 |
| 10065 | 8d9b66c9 | 진행/접수중 | 마감/접수마감 | JB 해외 바이어 상담회 |

---

## 신규 INSERT 2건

사이트 `work_year=2026` list 에 있는데 DB 미존재:

| spSeq | title | period | status |
|---|---|---|---|
| 93b55df1 | [교육/컨설팅] 2026년 FTA통상진흥센터 5차 교육 | 2026-04-27~05-04 | 마감 |
| 20338576 | [교육/컨설팅] 2026년 상반기 찾아가는 KOTRA 설명회 (4.15/전주) | 2026-04-10~04-14 | 마감 |

→ v1 DB jbexport 66 → 68건. (왜 이 두 row 가 자동 daily 에서 안 들어왔는지는 별건. 둘 다 마감 상태라 `filter_open_announcements` 단계 추정 — 즉 백로그 048 와 동일 원인의 또 다른 증상. 코드 fix 후로는 신규 마감 사업도 INSERT 자체가 안 되겠지만, 이미 마감 사업의 누락 백필은 본 정정으로 해결.)

---

## 운영(Render) 백필 SQL

`docs/render_sql/b047_b048_followup.sql` (5,487 bytes, 28 lines):
- 백로그 047 백필 4건 UPDATE (url 매칭, id 무관)
- 백로그 048 status 정정 4건 UPDATE
- 신규 INSERT 2건 (NOT EXISTS 가드)

Render Shell:
```bash
sqlite3 /var/data/biz.db < /var/data/b047_b048_followup.sql
```

---

## 산출물

### 신규 파일

- `pipeline/jbexport_daily.py` 수정 (selector 교체 + sync_status_to_db 추가)
- `scripts/debug/jbexport_site_vs_db_diff.py` (진단)
- `scripts/debug/jbexport_titleview_audit.py` (오작동 증명)
- `scripts/debug/jbexport_detail_html_dump.py` (HTML 분석)
- `scripts/debug/jbexport_title_selector_test.py` (selector 검증)
- `scripts/debug/jbexport_b047_backfill.py` (4건 백필)
- `scripts/debug/jbexport_b047_followup.py` (status + INSERT + SQL)
- `docs/backlog/048_jbexport_status_not_updated.md`
- `docs/render_sql/b047_b048_followup.sql`
- `docs/daily/2026-05-10.md` (이 파일)

### 수정 파일

| 파일 | 내용 |
|---|---|
| `pipeline/jbexport_daily.py` | selector 교체 (147-167) + sync_status_to_db 함수 (1062+) |
| `db/biz.db` | 백필 4건 + status 4건 + 신규 2건 (총 10 row 영향) |

---

## 핵심 교훈

1. **백필 검증의 함정** — 5/8 백로그 035 dry-run 에서 4 sample (4/23, 4/24, 5/8, bad spSeq) 모두 통과했지만, **모두 같은 detail 페이지의 같은 위치의 selector 결과** 였음. 다양한 spSeq 의 cross-match 검증이 빠져있어 selector 오작동을 못 잡음. 진단 신호("동일 title 두 row" → 5/8 일지 line 79-80)가 있었으나 분석 안 했음.

2. **사용자 시각의 중요성** — 사용자가 운영 사이트와 원본을 비교해서 "거의 일치하지 않음" 한 마디 → 5/8 chain (위젯 #1 깨진 제목 → DB stale → proxy down) 패턴 또 한 번 반복.

3. **알림 정책과 status sync 의 분리** — `filter_open_announcements` 가 메일/알림 흐름을 위한 필터이지만, status sync 도 같은 path 를 쓰면서 마감 사업의 status 갱신이 막혔음. 두 흐름 분리가 정답 (sync_status_to_db 별도 함수).

4. **selector 부수성 (parent 추적)** — `span.titleView` 같은 모호한 class 만으로 selector 잡으면 본문/nav 구분이 불가. 항상 parent chain (`ul.board_bottom` 등) 을 확인해서 의도한 위치인지 검증해야 함.

---

## 미해결 / 다음 세션 (오전 시점)

- 🟡 **운영(Render) SQL 적용** — `docs/render_sql/b047_b048_followup.sql` 사용자가 Render Shell 에서 실행 필요
- 🟢 **다른 source 동일 패턴 점검** — bizinfo/kstartup/jbbi/jbtp 도 status stale 가능성. backlog 048 과 동일 메커니즘인지 확인
- 🟢 **백로그 044 (DB_PATH)** / **045 (v1/v2 분리)** — 5/8 일지에서 이월

---

# 오후 — 백로그 049 (jbexport 위젯 정렬) + 050 (organization 추출)

## 한 줄 요약

사용자 진단으로 사이트 정렬 키 (notiChk DESC, oder DESC) 확정 → 049 컬럼 2개 추가 + ORDER BY 정식 매핑 + 68건 백필. 작업 중 위젯 4위 누락 사고 발견 → 050 organization 백필 (66/66 OK) + 위젯 필터 옵션 A 채택. v1/v2 양쪽 commit + push 완료.

---

## 백로그 049 — 위젯 정렬 정식 fix

### 배경 (백로그 034 한계)
백로그 034 (5/7) 가 `notice_create_dt` 컬럼을 추가했지만 update_db.py 의
`_prepare_row` / `_upsert_one` 이 해당 키를 INSERT/UPDATE 컬럼 목록에 넣지
않아 모든 행 NULL 상태. 5/8 일지에서 "5건 모두 4/23 13:19:09 batch +
notice_create_dt NULL → 사실상 id DESC 순서" 로 진단 마무리됨.

### 사용자 진단 (raw JSON 5건 검증)
사이트 jbexport.or.kr 정렬 키:
- 1차: `notiChk DESC` (공지 핀)
- 2차: `oder DESC` (등록 연번)

5건 검증:
```
notiChk=1: oder=1514 (912회), 1472 (1320회)
notiChk=0: oder=1544 (42회), 1543 (66회), 1542 (599회)
```

### 변경 (v1 + v2 동시)

| 파일 | 변경 |
|---|---|
| appy.py | `_init_db` 에 `notice_chk` / `notice_order` INTEGER DEFAULT 0 ensure 추가, `load_latest_by_source` jbexport 분기 ORDER BY 를 두 컬럼 매핑으로 교체 |
| pipeline/jbexport_daily.py | `extract_announcement` 에서 `notiChk` / `oder` 정수 추출 → out dict |
| pipeline/update_db.py | `_init_db` 에 직접 ALTER (DEFAULT 0 화이트리스트 미통과 우회), `_prepare_row` 정수 정규화, `_upsert_one` INSERT/UPDATE 두 컬럼 추가, 새 값 0 일 때 옛 값 보존 머지 (jbexport 외 source upsert 가 정렬 키 0 으로 덮는 사고 방지) |

### 백필 (release/2026-05-10_jbexport_widget_sort/)
- DB 백업: `biz.db.backup_20260510_160330_049_widget_sort_fc950f53ca35`
- 사이트 list API 직접 호출 (work_year=2026 + 2025, length=200) → 201건 수집
- jbexport 68건 모두 SP_SEQ url 매칭 → notice_chk + notice_order UPDATE
- 결과: matched=68, updated=68, no_match=0

### 검증
- 사용자 진단 5쌍 (notiChk, oder) DB 매칭: 5/5 OK
- 위젯 SQL 1, 2, 3, 5, 6 위 사이트와 일치
- 사이트 4위 (oder=1543, id=22915) 만 위젯에서 빠짐 → 별 문제 발견 (백로그 050 진입)

### 산출 + push
- v1 commit `db32bfd` → push origin main (Render 자동 배포 트리거)
- v2 commit `02d48f2` → push origin main

---

## 백로그 050 — organization 추출 정상화 (049 후속)

### 발견 경위
049 위젯 검증 중 사이트 4위 (oder=1543, id=22915) 가 위젯에 안 나타남. 이유:
DB organization='(재)전북특별자치도 경제통상진흥원' 으로 정확 추출되어 있었지만,
위젯이 `organization='전북수출통합지원시스템'` 임시 필터를 사용 중이라 차단됨.

### v1 DB 분포 (049 직후 점검)
- 66건 fallback `전북수출통합지원시스템` (시스템명, 백로그 032-1 이전 INSERT)
- 1건 `(재)전북특별자치도 경제통상진흥원`
- 1건 `코트라 전북지원본부`

→ 추출 성공률 1.5%. 그러나 selector 자체는 10/10 작동 (Phase 2 진단). 진짜 문제는
update_db 머지 로직 부재 — fetch 일시 실패 시 fallback 으로 진짜 값 덮어씀.

### Phase 2~5 진행 (자율)

**Phase 2 — 진단**: 7건 sample detail HTML 검사 → 모두 `td.th='주관기관'` 정확 작동.
fallback 들은 백로그 032-1 이전 stale.

**Phase 3 — selector 보강**: regex fallback 1개 추가 (보험성). 10건 sample 100%.

**Phase 4 — 백필** (release/2026-05-10_jbexport_org_fix/backfill_organization.py):
```
total=66  fetched=66  fetch_error=0  extracted=66
updated_to_real=66
백업: biz.db.backup_20260510_162758_050_org_fix_582aa6e616dd
```

**Phase 5 — update_db merge**: `_FALLBACK_ORGS` set + `_is_fallback_organization()`,
`_upsert_one` SELECT 에 organization 추가, 새 값이 fallback 이고 옛이 진짜면 보존
(049 패턴 동일).

### Phase 6 — 위젯 필터 정책 (사용자 결정)
**옵션 A — 필터 제거** 채택:
- 옵션 A: WHERE source='jbexport' AND title 깨짐 차단만 (단순)
- 옵션 D: + url LIKE '%jbexport.or.kr%' (보험)
- 옵션 B: organization 화이트리스트 (9개) — 새 기관 추가 시 코드 수정
- 옵션 C: 현 정책 유지 → 백필 후 위젯 0건 (실효 X)

source='jbexport' 68/68 url 도메인 jbexport.or.kr — misclassification 0 → A 안전.

### 백필 후 분포 (68건)

| 건수 | 기관명 |
|---|---|
| 47 | (재)전북특별자치도 경제통상진흥원 |
| 6  | 코트라 전북지원본부 |
| 4  | 한국무역협회 전북지역본부 |
| 3  | 전주시 |
| 3  | 전북특별자치도 |
| 2  | 전북지방우정청 |
| 1  | 한국무역보험공사 전북지사 |
| 1  | 전주상공회의소 |
| 1  | (재)전북바이오융합산업진흥원 |

### 위젯 5건 — 사이트 1~5위 정확 일치

```
1. notiChk=1 oder=1514 (재)전북특별자치도 경제통상진흥원  '온라인전시관 콘텐츠 제작지원'
2. notiChk=1 oder=1472 (재)전북특별자치도 경제통상진흥원  '제3차 무역사절단(미국 LA, 뉴욕)'
3. notiChk=0 oder=1544 (재)전북특별자치도 경제통상진흥원  'FTA통상진흥센터 설명회'
4. notiChk=0 oder=1543 (재)전북특별자치도 경제통상진흥원  'FTA통상진흥센터 5차 교육' ← 049 누락 행 부활
5. notiChk=0 oder=1542 한국무역협회 전북지역본부          '미주/유럽/중동 언택트 마케팅'
```

### Phase 7 산출
- `release/2026-05-10_jbexport_org_fix/MANIFEST.md`
- `release/2026-05-10_jbexport_org_fix/backfill_organization.py`
- `docs/backlog/050_jbexport_organization_fallback.md`
- `docs/render_sql/b050_followup.sql` (점검 SQL)
- v1/v2 cherry-pick: appy.py + pipeline/jbexport_daily.py + pipeline/update_db.py

---

## 핵심 교훈 (049 + 050)

1. **백필 단계의 함정 (반복)** — 백로그 034 가 컬럼 ALTER + crawler 출력은 했지만
   update_db 의 INSERT/UPDATE SQL 컬럼 목록 갱신을 빠트려 사실상 dead column.
   "code applied" ≠ "data flowing". 다음 컬럼 추가 작업부터는 update_db._prepare_row
   + _upsert_one 의 INSERT/UPDATE SQL 동시 점검 체크리스트 필요.

2. **임시 필터의 수명 관리** — 위젯 organization='전북수출통합지원시스템' 필터는
   2026-05-07 jbbi 섞임 회피용 임시 워크어라운드였지만, 백로그 041 (jbbi misclassify
   해결) 이후 제거 안 되고 살아남아 049 fix 의 1건 누락 사고 유발. 임시 필터 마다
   "제거 조건 (백로그 041 완료 시)" 댓글 달면 추적 쉬움.

3. **머지 로직 일반 패턴** — 049 (notice_chk/notice_order) 와 050 (organization)
   둘 다 "새 값이 누락/fallback 인데 옛 값이 진짜면 보존" 패턴. 향후 신규 컬럼 추가
   시 _upsert_one 머지 로직 기본 점검 항목으로 추가.

4. **Phase 6 stop 패턴 효과** — 자동 진행이 아니라 옵션 비교표 + 시뮬레이션 + 사용자
   결정 stop 으로 나눈 게 좋았음. 옵션 C 가 실은 위젯 깨짐인 걸 사용자가 결정 단계
   에서 명확히 인지.

---

## 미해결 (오후 마감 시점)

- 🟡 **운영(Render) DB 백필** — 049 + 050 의 backfill_*.py 두 개 Render Shell 에서
  실행 필요. 048 의 b047_b048_followup.sql 과 함께 같이 진행
- 🟢 **백로그 044 (DB_PATH)** / **045 (v1/v2 분리)** — 5/8 일지에서 이월 (5/10 일정에 미포함)

---

# 저녁 — 백로그 051 (jbexport 공지 제외) + 052 (proxy/sync 임시 우회)

## 한 줄 요약

049 적용 후 사용자가 운영 위젯 5건 모두 공지로 채워진 것 직관 발견 → **051 (공지 제외, 연번만 노출)** 정식화. 동시에 운영 위젯이 사이트 66/65 누락 → **052 진단**: proxy 5/8~ down + 운영 DB sync 자동화 부재 = chain 사고. 임시 sync_two_rows.py 로 두 row 만 우회. **본격 해결 (B+D / A) 다음 주 명세화**.

## 백로그 051 — 위젯 공지 제외 (jbexport)

### 발견 (사용자 직관)
049 운영 적용 직후 위젯 5건 모두 `notice_chk=1` (공지) 채워짐. 사용자: "공지가 위젯 점령하면 일반 신규 공고가 안 보임" → 정책 결정.

### 변경
| 파일 | 변경 |
|---|---|
| `appy.py` | `load_latest_by_source` jbexport 분기 WHERE 에 `notice_chk=0` 추가, ORDER BY 는 049 그대로 (`notice_chk DESC` 제거 — 공지 자체가 빠지므로 의미 없음) |

5건 = 사이트 1~5위 일반글 정확 매칭.

### push
- v2 commit `b68a0be` → push origin main
- v1 commit `502c56c` → push origin main

## 백로그 052 — proxy down + 운영 sync 자동화 (임시 우회)

### 진단 chain
1. v1 로컬 DB jbexport 68건 ↔ 운영 DB 65~66건 stale (oder=1544, 1543 누락/깨짐)
2. 원인 A: **proxy 5/8~ down** — 백로그 036 wrapper 정상이지만 proxy 자체가 외부 이슈로 동작 안 함
3. 원인 B: **운영 DB sync 메커니즘 없음** — v2 코드는 push 로 배포되지만 DB row 는 운영이 자체 daily run 으로 채움. proxy down → 운영 daily 실패 → row 누적 정지

### 임시 우회 (단발성)
`release/2026-05-10_jbexport_v1_sync/sync_two_rows.py` — v1 로컬 정상 row 두 개 (id=20283, 22915) 를 hardcoded snapshot 으로 운영 DB UPSERT. 멱등.

### 본격 해결 옵션 (다음 주)
- **A**: proxy 를 Render service 로 이전
- **B**: 사이트 list API 직접 호출 (proxy 우회) — 049 백필 패턴 검증 완료
- **C**: v1 로컬 → 운영 DB 자동 push (네트워크 의존)
- **D**: Render Cron + B 결합 (가장 깔끔)
- 우선: **B 검증 (1~2일) → 통과 시 D (0.5일)**, 실패 시 A 검토

### 산출
- `docs/backlog/052_proxy_sync_resolution.md`
- `release/2026-05-10_jbexport_v1_sync/sync_two_rows.py`
- v2 commit `6f1fa6e` (명세 + 임시 sync) / v1 commit `992f20a`

---

# 심야 — 백로그 053 (jbtp 위젯 fix) — playbook 첫 활용

## 한 줄 요약

진단 시간 **80% 단축** (~3h → ~30min) — 5/10 새벽 정착시킨 `site_sort_audit.md` playbook 첫 적용. jbtp 위젯 0/5 매칭 → 추출 보강 + 정렬 키 + 122 row 백필 → 사이트 page1 일반글 1~5위 정확 매칭. follow-up 두 건 (backfill self-contained + Render IP 차단 우회) 추가 완성.

## 백로그 053 — 메인 fix

### 진단 (playbook §1~3 절차 적용)
| Phase | 발견 |
|---|---|
| §1 사이트 fetch | proxy 무관 (HTML, requests.get 200 OK), JSON list API 와 다름 |
| §2 정렬 키 | **td[0] seq DESC** (dataSid 비단조: seq=2198→dataSid=20137, seq=2196→dataSid=20152) |
| §3 위젯 차단 원인 | start_date 미추출 95/128 (74%) → `start_date >= '2026-01-01'` 필터 silent drop + 정렬 키 부재 |

### 사용자 결정 (Phase 6 stop)
| Q | 결정 |
|---|---|
| Q1 위젯 공지 정책 | B 공지 제외 (jbexport 051 동일, 일관성) — 공지 32/122 (26%) 포함 시 위젯 5/5 모두 공지 |
| Q2 v1 sync 범위 | X surgical 단순 fix — 백로그 029 (connector divergence) 통째 sync 보류 |

### 변경 (v2)
| 파일 | 변경 |
|---|---|
| `connectors/connector_jbtp.py` | `parse()` 에 `is_notice` / `reg_date_text` / `seq_text` 추출. `normalize()` 에 `start_date` (td[6]) / `notice_chk` / `notice_order` (공지=dataSid, 일반=seq). `COLS` 두 컬럼 추가 |
| `appy.py` | `load_latest_by_source` jbtp 분기: WHERE `notice_chk=0` + ORDER BY `notice_order DESC, created_at DESC, id DESC` |

### 백필 (v2 로컬)
- `release/2026-05-10_jbtp_widget_fix/backfill_jbtp.py` (사이트 9페이지 fetch + dataSid 매칭 + UPDATE)
- DB 백업: `biz.db.backup_20260510_205346_053_jbtp_widget_d20796d40565`
- 결과: matched=122, updated=122, no_match=6 (9페이지 외 옛 row, 정상)

### 분포 변화 (v2 로컬)
| 지표 | BEFORE | AFTER |
|---|---|---|
| total | 128 | 128 |
| notice_chk=1 | 0 | 32 |
| start_date 채워짐 | 33 | 122 |

### 위젯 시뮬 (사이트 page1 일반글 1~5위 정확 매칭)
seq **2198 / 2197 / 2196 / 2195 / 2194** ✓

### push
- v2 commit `a5a4fe4` (메인 fix)
- v1 commit `5d0de72`

## 053 follow-up #1 — backfill self-contained (백로그 029 보류 호환)

### 발견
v1 sync 시 backfill_jbtp.py 가 v2 connectors/connector_jbtp.py (4단계 분리, 백로그 030) import 의존. v1 은 legacy 단일파일 (백로그 029 통째 sync 보류) → import 불가.

### fix
backfill 스크립트를 self-contained 화 — connector import 제거하고 사이트 9페이지 fetch + dataSid 추출 로직을 스크립트 안에 내장. v1/v2 어디서든 standalone 실행 가능.

### push
- v2 commit `ee467fb`
- v1 commit `efaa1bb`

## 053 follow-up #2 — Render IP 차단 발견 + sync UPSERT 확장

### 발견 1: Render IP 차단
Render Shell 에서 backfill_jbtp.py 실행 → `ConnectTimeout: HTTPSConnectionPool(host='www.jbtp.or.kr')`. 운영 사이트 fetch 자체가 막힘 → **jbtp.or.kr 가 Render 해외 IP 차단** 추정 (한국 정부/공공기관 흔한 패턴).

→ HTTP 호출 없는 우회 필요.

### 발견 2: 운영 DB stale 15건 (UPDATE only sync 부족)
운영 jbtp 113건 ↔ v2 snapshot 128건. 차이 15 = 5/4~5/7 새 사이트 row. 052 와 동일 chain (proxy down → 운영 daily 정지). **UPDATE only sync 로는 누락 → UPSERT 확장 필요**.

### sync 스크립트 (`sync_jbtp_v1_to_render.py`)
- **데이터 출처**: v2 로컬 DB → `v1_jbtp_dump.json` snapshot (128 row, full schema)
- **UPDATE 경로** (운영 url ∈ snapshot): notice_chk + notice_order + start_date **3 필드만**
  - 049 merge 보호: 새 값 0/'' + 옛 값 비-0/비-'' → 옛 값 보존
  - 운영 row 의 다른 모든 필드 (title/organization/ai_summary/...) 보존 → 백로그 029 영향 0
- **INSERT 경로** (snapshot url ∉ 운영): full row (052 sync_two_rows 패턴, INSERTABLE_COLS 30개)
- **멱등성**: 같은 값이면 UPDATE 0
- **사이트 영향**: 0 (HTTP 호출 없음)

### v1 로컬 시뮬 (운영 113건 stale 재현)
v1 DB 사본 → 최신 15 url 삭제 → 113건 stale 시뮬:
```
matched: 113  updated: 107  unchanged: 6
inserted: 15  snapshot_only_skip: 0  no_match: 0
```
위젯 시뮬 top 5 = 사이트 seq **2198 / 2197 / 2196 / 2195 / 2194** 정확 매칭 ✓

### push
- v2 commit `ba5f8ca` (Render IP 차단 우회 + snapshot dump)
- v1 commit `aad84e7` + `f537427` (UPSERT 확장)

---

# Commit hash 흐름 (5/10 전체)

## v2 (이 폴더)

| commit | 내용 | 백로그 |
|---|---|---|
| `02d48f2` | 위젯 정렬 키 정식 매핑 | 049 |
| `346d785` | playbook site_sort_audit 표준 절차 정립 | (도구) |
| `8e9780e` | organization 추출 정상화 + 머지 보호 + 위젯 필터 제거 | 050 |
| `25097eb` | 050 명세 + 데일리 보강 | 050 |
| `b05f8ee` | playbook §4 050 사례 추가 | (도구) |
| `b68a0be` | 위젯 공지 제외, 연번만 | 051 |
| `6f1fa6e` | proxy/sync 본질 해결 명세 + 임시 sync | 052 |
| `a5a4fe4` | jbtp 위젯 정렬 + start_date 추출 (공지 제외) | 053 |
| `ee467fb` | jbtp backfill self-contained화 (029 호환) | 053 |
| `ba5f8ca` | Render IP 차단 우회 sync + snapshot dump | 053 |

## v1 (운영)

| commit | 내용 | 백로그 |
|---|---|---|
| `db32bfd` | 위젯 정렬 키 정식 매핑 | 049 |
| `3d0ab18` | playbook sync | (도구) |
| `b8df16f` | organization 추출 정상화 | 050 |
| `90d449c` | 050 명세 + 운영 SQL + release 패키지 | 050 |
| `7afb9c2` | playbook §4 sync | (도구) |
| `502c56c` | 위젯 공지 제외 | 051 |
| `992f20a` | 052 명세 + jbexport v1 sync | 052 |
| `5d0de72` | jbtp 위젯 정렬 키 + 공지 제외 | 053 |
| `096b4bf` | jbtp 백필 + 명세 + playbook 패치 sync | 053 |
| `efaa1bb` | jbtp backfill self-contained화 | 053 |
| `aad84e7` | Render IP 차단 우회 sync + snapshot dump | 053 |
| `f537427` | sync UPSERT 확장 (snapshot_only 15 INSERT) | 053 |

총 v2 10건 / v1 12건 = 22 commit (오전~심야).

---

# 운영 백업 보존 목록

Render Shell 에서 백필 / sync 실행 시 자동 생성되는 백업 (sha256 + 타임스탬프 suffix):

| 백업 파일 | 생성 시점 | 백로그 |
|---|---|---|
| `biz.db.backup_*_049_widget_sort_fd3d9e9c5534` | 049 backfill 실행 시 | 049 |
| `biz.db.backup_*_050_org_fix_87b69ccc6d75` | 050 backfill 실행 시 | 050 |
| `biz.db.backup_*_052_v1_sync_21e8188fa85b` | 052 sync_two_rows 실행 시 | 052 |
| `biz.db.backup_*_053_jbtp_v1sync_<hash>` | 053 sync_jbtp_v1_to_render 실행 시 | 053 |

→ 이상 4건 모두 운영 영구 보존 권장. roll-back 시 안전 망. (047/048 b047_b048_followup.sql 도 별도 백업 필요)

---

# 핵심 교훈 (5/10 종합)

1. **사용자 직관의 정확성** — 위젯 5건 모두 공지인 것 (051), 사이트 4위 누락 (050), 운영 사이트 vs 원본 불일치 (047) — 모두 사용자 한마디에서 출발. 데이터 검증 전에 화면 검증 우선.

2. **"가설 금지, 확정 후 진행" 원칙 검증** — 053 진단에서 "dataSid DESC 일 것" 가설 → raw HTML 직접 확인 → td[0] seq 가 진짜 정렬 키. 가설로 진행했으면 fix 가 사이트와 또 어긋났을 것.

3. **정렬 키 = 사이트 표시 컬럼** (내부 ID 아님) — jbtp td[0] seq, jbexport notiChk + oder. dataSid 단조 가정은 위험. playbook §2 핵심 절차로 정착.

4. **v1 ≠ v2 ground-truth 충돌 검증 필수** — 053 sync 시 v2 = 백필 ground-truth (정확), v1 = legacy connector (백로그 029 미해결) divergence 인지하고 surgical 만 적용. 통째 sync 했으면 v1 운영 회귀 위험.

5. **Render IP 차단 = 한국 정부/공공기관 흔한 패턴** — jbtp.or.kr 외에도 jbexport, jbbi 등 동일 가능성. 향후 Render-side daily 설계 시 **사이트 list API 직접 호출 (proxy 우회)** + **HTTP 호출 없는 sync (snapshot 패턴)** 두 가지가 표준 우회로.

6. **playbook 가치 검증** — site_sort_audit.md 첫 정착 (오후) → 첫 활용 (053, 심야). 진단 시간 ~3h → ~30min (80% 단축). 다음 사이트 (jbbi, kstartup, 등) 도 동일 절차 적용 가능.

7. **임시 우회의 명확한 후속 처리** — 052 sync_two_rows + 053 sync_jbtp_v1_to_render 모두 단발성 우회. 본질 해결 (B+D / A) 까지 매주 모니터링 필요. 임시 fix 가 영구 fix 로 stale 화 되는 것 경계.

---

# 다음 주 백로그

## 신규 등록 (이 일지에서 발생)

- **백로그 056** — jbexport + jbtp 위젯 템플릿 통합 (organization 표시 정책)
  - jbtp = "전북테크노파크" 통일 / jbexport = 9종 분포 → 표시 정책 옵션 A/B/C 명확화 후 진행
- **백로그 057** — jbtp 운영 DB 5/2~5/7 신규 누적 안된 원인 진단
  - v1 로컬 128 ↔ 운영 113 (15건 차이) → 운영 sync 메커니즘 식별 + 멈춘 원인
  - 052 (proxy + sync 자동화) 와 같은 chain 사고 가능성

## 기존 잔존

- **백로그 044 (DB_PATH)** / **045 (v1/v2 분리)** — 5/8 일지에서 이월
- **백로그 052** 본격 해결 (B 검증 → D 적용 또는 A 검토)
- **백로그 029** (v1/v2 connector divergence) — 053 surgical 로 jbtp 만 임시 회피, 통째 sync 잔존
- **백로그 010** (첨부 다운로드 시스템) — 큰 작업, 마감 후 검토
- **AI 요약 재시도** — 538/12k 부분 완료 (5/7 잔존)

---

# 다음 주 할 일 (2026-W19, 5/11 ~ 5/17)

## 🔴 최우선

### 1. 백로그 057 Phase 2 — 운영 DB sync 메커니즘 신규 구축

**옵션 A-3**: PC Task 확장 + Render API 엔드포인트

**단계**:

1. **Render `appy.py` 에 `/api/sync` POST 엔드포인트 신설**
   - Auth: `ADMIN_KEY` (기존 환경변수)
   - Body: `{ source, rows: [full row] }`
   - UPSERT (052/053 패턴 일반화)
   - Response: `matched / updated / inserted / skipped`

2. **`sync_all_to_render.py` (PC 측) 작성**
   - v1 로컬 DB → Render API POST
   - 백업 / 멱등 / dry-run 표준

3. **`auto_run.bat` 끝에 추가**

4. **첫 sync 시 5/4~5/10 누락 1,759건 일괄 처리**:
   - bizinfo 1,688건
   - kstartup 60건
   - jbbi 7건
   - jbtp_related 1건
   - at_global 3건

**영향**: 백로그 052 도 동시 close (동일 본질).
**시간**: 2~3 세션.

---

### 2. 백로그 058 신규 — v1 로컬 connector 정지 진단

**정지 source (5/5~5/8 cluster)**:
- jbtp 5/7 정지
- at_global 5/7 정지
- kstartup 5/8 정지
- kseafood 5/5 정지

wrapper proxy 5/8 down 보다 이전 → wrapper 무관.

**가설**: 사이트 변경 / connector break / IP 차단 / PC 환경.

**단계**:
1. 각 connector 직접 호출 테스트
2. 사이트 HTML 구조 변경 확인
3. 5/5~5/8 commit log 점검
4. fix + 백필

**시간**: 1~2 세션.

---

## 🟡 중요

3. 백로그 029 — v1↔v2 connector sync (jbtp 통째 sync 포함)
4. 백로그 045 — v1↔v2 코드 동기화 (5/8 이월, 029 와 함께)
5. 백로그 056 — jbexport + jbtp 위젯 템플릿 통합 (사용자 결정 필요)

## 🟢 시간 여유 시

6. 백로그 022 — 사업 확장 비전 (전북→전남→충남)
7. 백로그 016 — 공유하기 버튼
8. 백로그 018/019/020/021 — 우선순위 낮음

## ⚠️ 마감 압박

9. **경진대회 응모서 (~7/3, JBTP 주관)**
   - 작업 위치: `C:\Users\custo\proposaltest`
   - BizGovPlanner 운영과 완전 분리

## 정리

10. 운영 DB 백업 검증 후 삭제 (049/050/052/053)
11. transcript redact (`SMTP_PASS`, `ADMIN_KEY` 평문 노출)

---

## 5/10 마라톤 핵심 발견 (재확인)

- **deploy-004 (5/4) 가 sync 메커니즘 의도적 break** (`.gitignore` + persistent disk + `ensure_db_file` SKIP)
- **운영 DB 5/4~5/10 누락 1,759건** (모든 source 영향)
- **v1 로컬 4 source 정지 (5/5~5/8)** — 별도 원인 (wrapper 무관)

---

## Phase 2 시작점 — `/api/sync` 엔드포인트 설계 sketch

```python
# appy.py 추가
@app.route('/api/sync', methods=['POST'])
def api_sync():
    if request.headers.get('X-Admin-Key') != os.getenv('ADMIN_KEY'):
        return jsonify({'error': 'unauthorized'}), 401

    data = request.get_json()
    source = data.get('source')
    rows = data.get('rows', [])

    # UPSERT (052/053 패턴 일반화)
    matched = updated = inserted = skipped = 0
    for row in rows:
        # ... UPSERT logic
        pass

    return jsonify({
        'source': source,
        'matched': matched,
        'updated': updated,
        'inserted': inserted,
        'skipped': skipped,
    })
```

