# 2026-05-10 일지 — 백로그 047 + 048 (jbexport 표시 정확도)

## 한 줄 요약

5/9 발견된 "운영 사이트 jbexport 항목이 원본 jbexport.or.kr 과 거의 일치하지 않음" 진단 → **백로그 035 (5/8) 백필이 잘못된 selector 로 다른 사업의 title 을 덮어씌웠던 버그** 확정.
selector 교체(백로그 047) + status sync 누락 코드 fix(백로그 048) 동시 완성. v1 DB 백필 + Render SQL 산출.

---

## 진단 chain

### Phase A — 사이트 vs DB diff

`scripts/debug/jbexport_site_vs_db_diff.py` 작성. 사이트 `getWork1Search.do` 직접 POST → list raw 받아 DB 매칭.

| 항목 | 값 |
|---|---|
| 사이트 work_year=2026 list | 68건 |
| v1 DB jbexport | 66건 |
| 첫 50 사이트 row 매칭 | 매칭 48 / 미존재 2 / **status 불일치 다수** |

핵심 신호: 사이트 list 에서 "접수마감" 인 사업 다수가 DB 에서는 `status='진행'` 으로 stale.

### Phase B — selector 오작동 증명

`scripts/debug/jbexport_titleview_audit.py`: 7개 spSeq detail 페이지의 `span.titleView` 첫 매치 비교.

```
spSeq=9810ffef (예상: 미주/유럽/중동 언택트) → 잡힌 title = "FTA통상진흥센터 5차 교육"
spSeq=eb876dda (예상: FTA통상진흥센터 설명회) → "FTA통상진흥센터 5차 교육"
spSeq=4f3a1a39 (예상: 해외통상거점센터)       → "서울푸드 연계 상담회"
spSeq=611bf9a5 (예상: 수출애로 해소)         → "온라인전시관 콘텐츠 제작지원"
...
```

→ **모든 페이지에서 첫 `span.titleView` 가 그 사업의 title 이 아닌 인접 사업의 title**.

### Phase C — selector 위치 식별

`scripts/debug/jbexport_detail_html_dump.py`: HTML 구조 dump.

```
[span.titleView] count=2
  [0] parents=['dd', 'dl', 'li', 'ul.board_bottom', 'div.content']
  [1] parents=['dd', 'dl', 'li', 'ul.board_bottom', 'div.content']
```

→ **`ul.board_bottom` (이전/다음 게시물 navigation) 안의 인접 사업 title** 을 잡고 있었음.

진짜 본문 title 위치:
1. `<table>` 내부 `td.th="지원사업"` 옆 td (카테고리 prefix `[교육/컨설팅]` 등 포함)
2. 페이지 끝 `<script>$('title').text('... | 사업명')</script>`

### Phase D — 대안 selector 검증

`scripts/debug/jbexport_title_selector_test.py`: 7 spSeq × 2 selector.

| selector | 통과 |
|---|---|
| A) `td.th="지원사업"` 옆 td | 6/7 (1건은 whitespace normalize 차이뿐) |
| B) `<script>$('title').text(...)` regex | 7/7 |

A 채택 (카테고리 prefix 보존), 매칭 실패 시 B 로 fallback.

---

## 백로그 047 — selector 교체 + 백필

### 코드 수정 (pipeline/jbexport_daily.py:147-167)

```python
# before (백로그 035, 잘못된 selector)
title_tag = soup.select_one("span.titleView")

# after (백로그 047)
for tr in soup.select("table tr"):
    th_td = tr.select_one("td.th")
    if th_td and "지원사업" in th_td.get_text(strip=True):
        sib = th_td.find_next_sibling("td")
        if sib:
            for noise in sib.select("span.blue_txt, span.red_txt, ..."):
                noise.decompose()
            ...
            break
if not out["title"]:
    m = re.search(r"\$\('title'\)\.text\('([^']+)'\)", html)
    ...  # | split, 마지막 segment
```

### v1 DB 백필 4건

`scripts/debug/jbexport_b047_backfill.py`. 백업: `db/biz.db.backup_b047_pre_backfill_20260510_083439`.

| id | spSeq | BEFORE title | AFTER title |
|---|---|---|---|
| 10064 | 4f3a1a39 | [코트라] 서울푸드 연계 상담회 | [시장조사/지사화] [변경공고] 해외통상거점센터 |
| 10066 | 9fbace94 | 2026 JB 해외 바이어 | [기타 지원사업] (중동 긴급) 수출 물류비 |
| 12425 | 9810ffef | FTA통상진흥센터 5차 교육 | [온라인 마케팅] 미주/유럽/중동 언택트 |
| 20283 | eb876dda | FTA통상진흥센터 5차 교육 | [교육/컨설팅] 전북FTA통상진흥센터 설명회 |

start_date / end_date / period_text 도 사이트 진실로 재추출.

---

## 백로그 048 — status stale (동시 발견)

### 근본 원인

`pipeline/jbexport_daily.py:filter_open_announcements` (line 569-578) 가
`OPEN_STATUSES = {"접수중", "공고중"}` 외 항목을 today.json 에서 제거 →
update_db 흐름에 마감 사업이 도달하지 못함 → 기존 DB row 의 status 갱신 0.

`update_db._upsert_one` 자체는 status UPDATE 를 한다 — 단지 마감 row 가 그곳에 도달하지 못할 뿐.

### 코드 fix (pipeline/jbexport_daily.py:1062+)

신규 함수 `sync_status_to_db(all_items)`:
- url 정확 일치로 매칭
- `status` / `raw_status` 만 UPDATE
- 메일/알림 흐름 (today.json → update_db) 변경 없음

`run_daily()` 끝에 호출 추가.

검증: 단독 호출로 stale row 1건 → 마감 정정 OK.

### v1 데이터 정정 4건

| id | spSeq | BEFORE | AFTER | 사업명 |
|---|---|---|---|---|
| 10061 | 8260a0d4 | 진행/공고중 | **마감/접수마감** | 전주시 AI 마케팅 |
| 10062 | c02eb9e7 | 진행/접수중 | 마감/접수마감 | 중국 시장 테스트 마케팅 |
| 10063 | 1d902514 | 진행/접수중 | 마감/접수마감 | 서울푸드 상담회 |
| 10065 | 8d9b66c9 | 진행/접수중 | 마감/접수마감 | JB 해외 바이어 상담회 |

---

## 신규 INSERT 2건

사이트 `work_year=2026` list 에 있는데 DB 미존재:

| spSeq | title | period | status |
|---|---|---|---|
| 93b55df1 | [교육/컨설팅] 2026년 FTA통상진흥센터 5차 교육 | 2026-04-27~05-04 | 마감 |
| 20338576 | [교육/컨설팅] 2026년 상반기 찾아가는 KOTRA 설명회 (4.15/전주) | 2026-04-10~04-14 | 마감 |

→ v1 DB jbexport 66 → 68건. (왜 이 두 row 가 자동 daily 에서 안 들어왔는지는 별건. 둘 다 마감 상태라 `filter_open_announcements` 단계 추정 — 즉 백로그 048 와 동일 원인의 또 다른 증상. 코드 fix 후로는 신규 마감 사업도 INSERT 자체가 안 되겠지만, 이미 마감 사업의 누락 백필은 본 정정으로 해결.)

---

## 운영(Render) 백필 SQL

`docs/render_sql/b047_b048_followup.sql` (5,487 bytes, 28 lines):
- 백로그 047 백필 4건 UPDATE (url 매칭, id 무관)
- 백로그 048 status 정정 4건 UPDATE
- 신규 INSERT 2건 (NOT EXISTS 가드)

Render Shell:
```bash
sqlite3 /var/data/biz.db < /var/data/b047_b048_followup.sql
```

---

## 산출물

### 신규 파일

- `pipeline/jbexport_daily.py` 수정 (selector 교체 + sync_status_to_db 추가)
- `scripts/debug/jbexport_site_vs_db_diff.py` (진단)
- `scripts/debug/jbexport_titleview_audit.py` (오작동 증명)
- `scripts/debug/jbexport_detail_html_dump.py` (HTML 분석)
- `scripts/debug/jbexport_title_selector_test.py` (selector 검증)
- `scripts/debug/jbexport_b047_backfill.py` (4건 백필)
- `scripts/debug/jbexport_b047_followup.py` (status + INSERT + SQL)
- `docs/backlog/048_jbexport_status_not_updated.md`
- `docs/render_sql/b047_b048_followup.sql`
- `docs/daily/2026-05-10.md` (이 파일)

### 수정 파일

| 파일 | 내용 |
|---|---|
| `pipeline/jbexport_daily.py` | selector 교체 (147-167) + sync_status_to_db 함수 (1062+) |
| `db/biz.db` | 백필 4건 + status 4건 + 신규 2건 (총 10 row 영향) |

---

## 핵심 교훈

1. **백필 검증의 함정** — 5/8 백로그 035 dry-run 에서 4 sample (4/23, 4/24, 5/8, bad spSeq) 모두 통과했지만, **모두 같은 detail 페이지의 같은 위치의 selector 결과** 였음. 다양한 spSeq 의 cross-match 검증이 빠져있어 selector 오작동을 못 잡음. 진단 신호("동일 title 두 row" → 5/8 일지 line 79-80)가 있었으나 분석 안 했음.

2. **사용자 시각의 중요성** — 사용자가 운영 사이트와 원본을 비교해서 "거의 일치하지 않음" 한 마디 → 5/8 chain (위젯 #1 깨진 제목 → DB stale → proxy down) 패턴 또 한 번 반복.

3. **알림 정책과 status sync 의 분리** — `filter_open_announcements` 가 메일/알림 흐름을 위한 필터이지만, status sync 도 같은 path 를 쓰면서 마감 사업의 status 갱신이 막혔음. 두 흐름 분리가 정답 (sync_status_to_db 별도 함수).

4. **selector 부수성 (parent 추적)** — `span.titleView` 같은 모호한 class 만으로 selector 잡으면 본문/nav 구분이 불가. 항상 parent chain (`ul.board_bottom` 등) 을 확인해서 의도한 위치인지 검증해야 함.

---

## 미해결 / 다음 세션

- 🟡 **운영(Render) SQL 적용** — `docs/render_sql/b047_b048_followup.sql` 사용자가 Render Shell 에서 실행 필요
- 🟢 **다른 source 동일 패턴 점검** — bizinfo/kstartup/jbbi/jbtp 도 status stale 가능성. backlog 048 과 동일 메커니즘인지 확인
- 🟢 **백로그 044 (DB_PATH)** / **045 (v1/v2 분리)** — 5/8 일지에서 이월
