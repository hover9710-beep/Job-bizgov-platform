# 2026-05-13 일지 — 5/12 b064+#1 활성화 자동화 1차 검증 PASS + 백로그 065 Phase 2-A 진단 종합 종료

## 한 줄 요약

저녁 PC daily run 1차 검증 6항목 — run_aux 4 connector 자동 호출 ✅, jbtp +2 / jbexport +1 / bizinfo +1246 신규 정상, at_global 시도 205/신규 0/중복 205 = URL 정규화 idempotent 결정적 증거 확보 (백로그 064 fix 실효성 검증 통과), 사고 #2/#5 재발 0건. 그러나 #4 Render sync 자동 동작 검증에서 **auto_run.bat 에 sync step 자체 누락** 발견 → 5/13 신규 1,250 row 전부 `synced_to_render=0`, 운영 위젯 jbtp top notice_order=20248 (5/12 그대로) = sync 미발동 시각 증거. 한밤중 ~ 새벽 백로그 065 Phase 2-A 진단 4항목 — `raw_status` 사용 범위 (appy.py 13곳 READ + connector 2 WRITE + jbexport_daily UPDATE 실패 지점), `_safe_add` 리스트 5건 누락 (`synced_to_render`/`synced_at`/`notice_chk`/`notice_order`/`notice_create_dt`), yaml 진단 8건 (continue-on-error 전수, `/api/sync` 0개, schema ensure 부재, "Ensure DB file" step 사라짐, timeout 여유 0 등), `sync_to_render.py` 인터페이스 (env `ADMIN_KEY`+`RENDER_URL`+`DB_PATH`, POST body-auth `{key,source,rows}`, exit code fatal-only 1, schema dependency `synced_to_render`/`synced_at`). Phase 2-B 스코프 5건 (어제 추정 2건의 ~2.5배). 사고 0, fix 0, commit 0 — 사이클 분리 완벽.

---

## 저녁 — 자동화 검증 1차 (#1~#3, #6)

### #1 PC daily run 실행 여부

| 항목 | 결과 |
|---|---|
| fire 시각 | `2026-05-13 20:37:58 pid=30020` (Task Scheduler 20:37 정확) |
| wrapper 백로그 036 effect | `[run_all] JBEXPORT proxy OK: http://127.0.0.1:5001` (proxy 자동 시작 정상) |
| 종료 시각 | `20:42:59` (5분 1초 소요) |
| **wrapper exit** | **`run_all exit=0`** (백로그 046 has_fatal 검사 효과) |
| 로그 파일 | `logs/run_all_2026-05-13.log` (134KB, 1953줄) |

#### 5/9 이후 wrapper exit 추세 (백로그 046 누적 효과)

| 날짜 | run_all exit | 비고 |
|---|---:|---|
| 5/8 ×3회 | 1 | 백로그 046 적용 전 (Task 변경 검증) |
| **5/9 20:37** | **0** | 백로그 043+046 첫 적용 |
| 5/10 / 5/11 / 5/12 / **5/13** | 0 | 5일 연속 |

→ 백로그 042 alert 시스템 false-positive 0건 ✅

#### 단계 5-7 mail chain 결과

| 단계 | line | exit | 결과 |
|---|---:|---:|---|
| 5) Make mail body | 2263 | 0 | ✅ |
| 5a) Kakao token refresh | 2278 | (skip) | KAKAO_REFRESH_TOKEN 미설정 → 의도된 skip |
| 5b) Make Kakao body | 2285 | 0 | ✅ |
| **5c) Kakao notify (send)** | 2292 | **1** | ❌ **단독 non-fatal** (KAKAO_REFRESH_TOKEN 부재 환경에서 항시 실패 패턴 — 별건) |
| 6) Mailer | 2301 | 0 | ✅ `[정보] users DB 발송 대상 없음 → 정상 종료 (백로그 043)` |
| 7) Mailer (real send) | 2319 | 0 | ✅ 동상 |

→ 5c 단독 non-fatal 만 실패. 백로그 046 `has_fatal` 분기로 wrapper exit=0 유지 ✅

### #2 DB 변동 (BEFORE 5/12 → AFTER 5/13)

| source | before | after | delta |
|---|---:|---:|---:|
| jbtp | 137 | **139** | **+2** ✅ 사이트 신규 정확 캡처 |
| jbexport | 70 | 71 | +1 |
| bizinfo | (5/12 EOD) | (5/13) | **+1,246** |
| kstartup | - | - | +1 |
| jbbi | 372 | 372 | +0 (사이트 변동 없음) |
| at_global | 206 | 206 | +0 (사이트 1건 삭제, DB 잔존) |
| jbtp_related | 72 | 72 | +0 (사이트 2건 삭제, DB 잔존) |
| kseafood | 244 | 244 | +0 (run_aux 미포함 — 예상대로) |

### #3 URL 정규화 idempotent 검증 (백로그 064 fix 실효성)

| connector | 시도 | 신규 | 중복 | exit | 의미 |
|---|---:|---:|---:|---:|---|
| jbtp | 128 | **2** | 126 | 0 | 사이트 신규 2건 정확 추출 |
| jbbi | (372) | 0 | (372) | 0 | 사이트 변동 0 |
| **at_global** | **205** | **0** | **205** | 0 | **URL 정규화 idempotent 결정적 증거** |
| jbtp_related | (70) | 0 | (70) | 0 | 사이트 신규 0 |

→ at_global 205/0/205 = **사고 #5 (4 connector 동시 대량 중복) 재발 0건** 확정. 백로그 064 fix 가 정확히 의도대로 작동.

### #6 위험 시나리오 재발 점검

| 시나리오 | 결과 |
|---|---|
| 사고 #2 (url 파라미터 변경) 재발 | ❌ 0건 — 4 connector 모두 정상 fetch + parse |
| 사고 #5 (4 connector 동시 대량 중복) 재발 | ❌ 0건 — at_global idempotent 결정적 |
| 새 위험 (사이트 변경) | at_global 1건 / jbtp_related 2건 사이트 측 **delete** 감지 (별건 — DB sync_status 정책 검토 대상) |

#### 별건 (보고만, fix X)
- **콘솔 인코딩 깨짐**: cp949 → UTF-8 파일 저장 mojibake. 데이터 무결성 영향 0.
- **site shrinkage**: at_global -1, jbtp_related -2 사이트 측 삭제. DB sync_status 정책 별건.
- **5c Kakao 항구 실패**: KAKAO_REFRESH_TOKEN 부재 환경. 별건.

---

## 저녁 — 자동화 검증 2차 (#4 Render sync)

### 🚨 핵심 발견 — sync step 자체가 auto_run.bat 에 누락

#### auto_run.bat 5단계 wrapper 구성 (line 1-72)

| step | 코드 라인 | 동작 |
|---|---:|---|
| 1 | 20-24 | proxy alive check |
| 2 | 27-38 | proxy auto-start (백로그 036) |
| 3 | 50-53 | `py run_all.py >> %LOG% 2>&1` |
| 4 | 55-63 | proxy cleanup (started_by_us only) |
| 5 | 65-70 | alert on exit != 0 (백로그 042) |

→ **opt-in sync step 미존재**. 백로그 057 Phase 2.1f release 후보 (`0a88aff`) 의 wrapper 변경이 v1 auto_run.bat 정식 cherry-pick **미완료**.

#### 5/13 wrapper log grep

```
(sync_to_render|/api/sync|\[sync|SYNC|synced_to_render)
→ No matches
```

#### synced_to_render 컬럼 분포

| 전체 | count |
|---|---:|
| synced=0 | **17,026** |
| synced=1 | 207 |

#### source 별

| source | synced=0 | synced=1 |
|---|---:|---:|
| jbexport | 1 | 70 |
| jbtp | 2 | 137 |
| jbbi | 372 | 0 |
| at_global | 206 | 0 |
| jbtp_related | 72 | 0 |
| kseafood | 244 | 0 |
| bizinfo | 15,625 | 0 |
| kstartup | 504 | 0 |

#### 5/13 신규 row sync flag

| source | synced=0 | synced=1 |
|---|---:|---:|
| bizinfo | 1,246 | 0 |
| jbtp | 2 | 0 |
| jbexport | 1 | 0 |
| kstartup | 1 | 0 |

→ **5/13 신규 전체 미동기**. synced_at MIN=`2026-05-12 11:52:44` MAX=`2026-05-12 13:25:34` — 가장 최근 sync 는 5/12 점심~오후 한 차례만 (Phase 2.1d/2.1e 시점).

---

## 밤 — 위젯 검증 (#5)

### v1 측 위젯 시뮬 — top 5 per source

#### jbtp ✅ 5/13 신규 2건이 top 1·2 정확 점유

| rank | id | notice_order | cdate | title |
|---:|---:|---:|---|---|
| **1** | 26594 | **20256** | 2026-05-13 | 2026년 홀로그램 산업 확산지원 사업 통합공고 |
| **2** | 26595 | **20253** | 2026-05-13 | 2026년 스케일업 공공실증 지원사업 참여기업 모집 공고 |
| 3 | 25046 | 20248 | 2026-05-12 | 전주 스마트그린산단 산업단지 에너지 자급자족형 인프라 구축사업 |
| 4 | 25047 | 20246 | 2026-05-12 | 2026년 전북 지역기업 성장사다리 지원사업 추가 모집공고 |
| 5 | 25048 | 20223 | 2026-05-12 | 2026년도 전북 지역특화형 스마트공장 구축지원 사업 공고 |

#### jbexport ✅ 5/13 신규 1건이 top 1

| rank | id | notice_order | cdate | title |
|---:|---:|---:|---|---|
| **1** | 28615 | **1547** | 2026-05-13 | [기타 지원사업] (중동 긴급) 2026 수출 물류비 지원사업 추가모집 |
| 2 | 24225 | 1546 | 2026-05-11 | [온라인 마케팅] 전북AI무역지원센터 참가기업 모집 |
| 3 | 24226 | 1545 | 2026-05-11 | [교육/컨설팅] 2026년 전북FTA통상진흥센터 1차 설명회 |

→ 5/12 정상화 결과 (jbtp max=20248, jbexport max=1546) 유지 ✅ + 5/13 신규 정상 적층 ✅

#### jbbi / at_global / jbtp_related (5/13 신규 0건)
- 세 source 모두 `notice_order=0` 일관 (connector 가 미사용) — id DESC fallback 으로 cdate 최신순 유지

### 시나리오 판정

| 시나리오 | 판정 |
|---|---|
| A (v1 정상 + 운영 정상) | ❌ 불가능 (sync step 누락) |
| **B** (v1 정상 + 운영 5/12 그대로) | 🔵 **확정** — #4 결과와 정합. 운영 위젯 jbtp top notice_order = **20248** (5/12) = sync 미발동 시각 증거 |
| C (v1 자체 5/13 신규 안 보임) | ❌ 배제 — 5/12 사고 #1 (merge_jb drop) 재발 0건 |

---

## 한밤중 ~ 새벽 — 백로그 065 Phase 2-A 진단 (#1~#4)

### #1 `raw_status` 사용 범위 grep

#### 코드 (v1 main, active)

| 파일 | 라인 | 역할 |
|---|---:|---|
| **appy.py** | **329** | **defensive ALTER (이미 존재)** — `_safe_add` |
| appy.py | 137, 2798 | row → dict 컬럼 리스트 |
| appy.py | 622 | display logic |
| appy.py | 1387, 1448, 1552, 1717, 1735, 1771, 2208, 2652 | SELECT 쿼리 (위젯/상세/관리) |
| **pipeline/jbexport_daily.py** | **1102** | **`sync_status_to_db()` UPDATE** — Phase 1 실패 지점 |
| pipeline/flask_ui_audit.py | 73 | SELECT (audit) |
| connectors/connector_at_global.py | 93, 113, 181 | crawler → row dict 에 raw_status 기록 |
| connectors/connector_kseafood.py | 113, 152, 250 | crawler → row dict 에 raw_status 기록 |

→ **READ 7+ 위치, WRITE/UPDATE 4 위치, 전부 컬럼 존재 전제**. appy.py:329 `_safe_add` 가 boot 시점에만 발동 → Actions crawler path 에서는 미발동.

#### sync_to_render.py (b057 Phase 2.1c, release 후보)

`release/2026-05-11_b057_phase21f_wrapper/pipeline/sync_to_render.py:91` — payload 컬럼 25개 리스트에 `raw_status` 포함. 즉 Render 측 DB schema 에도 `raw_status` 존재 필요 (현재는 appy.py:329 가 server boot 시 ALTER 한 결과로 OK).

#### connector 측 미사용 (4 source)

`raw_status` write 하는 connector 는 **at_global + kseafood 둘 뿐**. 나머지 (jbtp, jbbi, jbtp_related, jbexport, bizinfo, kstartup) 은 raw_status 미기록. jbexport 는 `sync_status_to_db` 별도 함수에서 보정 UPDATE.

### #2 옵션 A 의 구체 위치 결정

#### distinct connector 카운트 = **3** (at_global, kseafood, jbexport)

| 후보 | 위치 | 적합 조건 |
|---|---|---|
| a | `pipeline/jbexport_daily.py` 의 `sync_status_to_db()` 진입부 | 1개 connector |
| **b** | **`pipeline/_ensure_schema.py` 신설 + 각 connector import** | **2~3개 connector ✅** |
| c | Actions yaml init step + `appy._ensure_schema()` 호출 | ≥4개 또는 Actions 전용 처리가 합리적 |

→ **권고: b** (count rule 정합)

#### 보충 — appy.py `_safe_add` 리스트가 outdated

appy.py:319-346 의 23 컬럼 리스트에 **5건 누락**:

| 컬럼 | 추가 위치 | 백로그 |
|---|---|---|
| `synced_to_render` | docs/daily/2026-05-11.md:240 수동 SQL | b057 Phase 2.1a |
| `synced_at` | docs/daily/2026-05-11.md:241 수동 SQL | b057 Phase 2.1a |
| `notice_chk` | pipeline/update_db.py:101 (run_all 4d step) | b047 |
| `notice_order` | pipeline/update_db.py:106 (run_all 4d step) | b047/057 |
| `notice_create_dt` | release/2026-05-10_*/ 1회성 script | b034 |

→ schema 마이그레이션이 **5+ 위치에 fragmented**. `_ensure_schema.py` 가 28+ 컬럼 통합 책임을 갖는 게 자연스러움.

#### 최종 권고: **b (메인) + c-light (yaml 보충, optional)**

`_ensure_schema.py` 작성 후 Actions yaml init step 에서도 호출 → 방어 깊이 확보. connector 가 import 누락해도 yaml step 이 보장.

### #3 `daily-crawl.yml` 전체 review

#### 현 구조 (129줄)
| 항목 | 값 |
|---|---|
| schedule | `cron: "0 21 * * *"` (UTC) = **KST 06:00** ✅ |
| trigger | schedule + `workflow_dispatch` ✅ |
| runner | `ubuntu-latest` |
| timeout-minutes | 60 (전체), 5~15 (스텝별) |
| permissions | `contents: write` (현재 미사용) |
| Python | 3.12 (PC 3.14 와 minor skew) |

#### 스텝 순서 (8 crawl + 1 summary, 전부 `continue-on-error: true`)

bizinfo → kstartup → at_global → kseafood → jbbi → jbtp → jbtp_related → jbexport(proxy) → summary

#### 🚨 진단 8건

| # | 발견 | 의미 |
|---:|---|---|
| 1 | **`continue-on-error: true` 전수 적용** | Actions UI "Success" 거짓 9일간의 정확한 코드 근거 ✅ |
| 2 | "Ensure DB file exists" step 부재 | 5/5 commit `6ffbd1c` 에 있었던 step 사라짐 (별건) |
| 3 | **schema ensure step 부재** | Actions 빈 DB → raw_status 부재 → exit 1 의 진짜 fix point |
| 4 | **`/api/sync` POST step 0개** | Phase 2 의 핵심 누락 |
| 5 | DISABLED 코멘트 (5/3 disable) | historical context — 새 sync path 가 위에 추가될 위치 |
| 6 | JBEXPORT proxy nohup 패턴 | PC auto_run 등가 |
| 7 | 순서: bizinfo 첫, jbexport 마지막 | proxy 의존 격리 |
| 8 | **timeout 합계 = 정확 60분** | 여유 0, sync step 추가 시 75분 상향 권고 |

### #4 `sync_to_render.py` 인터페이스 확인

#### 환경변수 (정확한 이름)
| env | 기본값 | 역할 |
|---|---|---|
| **`ADMIN_KEY`** | `dev-admin-key` | **/api/sync 인증 키** |
| `RENDER_URL` | `https://job-bizgov-platform.onrender.com` | sync 대상 base URL |
| `DB_PATH` | `db/biz.db` | SQLite 경로 |

→ #3 추정 (`RENDER_SYNC_TOKEN`/`RENDER_SYNC_URL`) 는 코드에 없음. **ADMIN_KEY 확정** (메모리 표준 일치).

#### /api/sync 호출 명세
- URL: `{RENDER_URL}/api/sync`
- Method: POST
- **Auth: body 의 `key` 필드** (HTTP header 아님)
- Payload:
  ```json
  {
    "key": "<ADMIN_KEY>",
    "source": "<source name>",
    "rows": [<25-field whitelist row>...]
  }
  ```
- Timeout: 60s (Render cold start 고려)
- 응답 기준: HTTP 200 + `body.ok == true`

#### 페이로드 컬럼 whitelist (25개)
url, title, organization, start_date, end_date, status, description, source, site, collected_at, ministry, executing_agency, receipt_start, receipt_end, biz_start, biz_end, raw_status, attachments_json, period_text, apply_url, pdf_path, notice_create_dt, notice_chk, notice_order

→ 운영 enrich 컬럼 (`view_count`, `ai_*`, `recommend_*`, `attachment_text`, `score`, `reason`, `synced_*`, `id`, `created_at`, `updated_at`) 전송 안 함.

#### CLI 인터페이스
| flag | 기본 | 역할 |
|---|---|---|
| `--db`, `--render-url`, `--admin-key` | env 또는 default | override |
| `--source <src>` | (반복 가능) | source 필터 |
| `--batch-size <N>` | 500 | |
| `--timeout <N>` | 60 | |
| **`--dry-run`** | (flag) | POST + DB UPDATE 안 함 ✅ |

#### 실패 시 exit code 정책
| 종료 | 조건 |
|---:|---|
| 0 | 정상. batch-level 실패가 있어도 0 (멱등 재시도) |
| 1 | DB 없음 / ADMIN_KEY 빈 / **`synced_to_render`+`synced_at` 컬럼 부재** (fatal-only) |

→ Phase 2-B aggregation 함의: yaml 에서 exit code 만 보면 batch failure silent. **stdout 파싱** (`batches_fail` 값) 또는 batch 실패 시 exit 1 변경 검토 필요.

#### 🚨 schema dependency (라인 232-240)

```python
if "synced_to_render" not in cols or "synced_at" not in cols:
    print("...→ scripts/migrate_add_sync_columns.py 먼저 실행")
    return 1
```

→ sync_to_render.py 자체가 두 컬럼 부재 시 exit 1. **schema migration step 이 sync 호출 전에 필수**. `pipeline/_ensure_schema.py` 가 raw_status + synced_to_render + synced_at + 추가 5건 모두 책임지는 통합 모듈이어야 함.

### Secrets 목록 확정 (사용자 작업)

| Secret name | 필수성 | Value 출처 |
|---|---|---|
| **`ADMIN_KEY`** | ✅ 필수 | Render Dashboard 환경변수 `ADMIN_KEY` 와 **동일 값** |
| `RENDER_URL` | ❌ 불필요 | 코드 default 가 운영과 동일 |
| `DB_PATH` | ❌ 불필요 | Actions 기본 `db/biz.db` 정확 |

→ **순수 추가 = ADMIN_KEY 1개**. 위치: hover9710-beep/Job-bizgov-platform → Settings → Secrets and variables → Actions → New repository secret.

⚠️ ADMIN_KEY 값 본 일지에 노출 안 함 (정책 준수).

---

## 🟨 사용자 결정 사항 4건 (Phase 2-B 진입 전 사전 조건)

| # | 항목 | 권고 / 사실 | 결정 |
|---:|---|---|---|
| (1) | 옵션 A 최종 확정 | 사실상 확정 (B/C 비교에서 A 명백 우세) | ☐ |
| (2) | 후보 a/b/c 결정 | **b (메인) + c-light (yaml 보충)** 권고 | ☐ |
| (3) | Secrets 추가 시점 + 목록 | **ADMIN_KEY 1개**, Phase 2-B 적용 직전 추가 | ☐ |
| (4) | Phase 2-B 진행 시점 | **W20 fresh 사이클** (`feedback_cycle_separation.md`) — 지금은 아님 | ☐ |

---

## 메타 평가 — 사이클 분리 완벽

| 지표 | 5/12 마라톤 | **5/13** |
|---|---:|---:|
| 사고 발생 | 5건 (사고 #1 merge_jb drop / #2 jbtp url 파라미터 / #3-#5) | **0건** |
| inline fix 시도 | (사이클 내 발견 → 사이클 내 fix) | **0건** |
| commit / push (code) | 5+ commits | **0건** |
| 새 백로그 신설 | 061, 062, 063, 064, 065 | **0건 (065 진행만)** |
| 진단 범위 격상 | "stale 해결" → "5/3 disable 의 대체" | "raw_status 한 줄 fix" → **"schema 인프라 통합 + Actions 자동화 완성"** |

→ `feedback_cycle_separation.md` 정책 ("스코프 확대 발견 시 inline fix X, 신규 백로그 + 다음 사이클 이월") **완전 준수**. 모든 fix 결정 사용자 손에 위임.

### 5/13 의 가치

1. **자동화 검증 1차** — 5/12 한밤중에 활성화한 `run_aux_crawlers` (b064+#1, `9b9398a`) 의 첫 24h 운영 검증. **PASS** (idempotent 결정적 증거 + 사고 재발 0).
2. **시각적 sync 미발동 증거 확보** — 운영 위젯 jbtp top notice_order=20248 (5/12 그대로). b057 Phase 2.1f wrapper sync step 이 v1 정식 cherry-pick 안 됨 = 핵심 누락 확정.
3. **백로그 065 Phase 2-A 종합 종료** — Phase 1 (5/12 새벽) 의 raw_status 단일 문제로 보였던 것이 실제는 schema 마이그레이션 **5+ 위치 fragmented** + yaml 8건 진단 + Secrets 1건 = Phase 2-B 스코프 5건 (어제 추정 2건의 ~2.5배 확대).
4. **체계 정립** — Phase 2-A 결과가 Phase 2-B 입력 자료로 정형화. W20 사이클 진입 시 명확한 작업 단위 + 결정 사항 4건 + 권고 (b + c-light) 보유.

---

## 5/13 회고 — raw_status 빚의 본질 + 향후 방향

### raw_status 정의 + 역할

> 사이트 원본 상태값을 손대지 않고 저장하는 칸. `status` (통일된 표준 값) 의 *출처이자 안전망*.

- 사이트마다 표현 다양 (bizinfo "접수중" / kstartup "모집중" / jbtp "진행중" / at_global "OPEN" / "상시접수")
- 흐름: `raw_status` (원본) → `infer_status()` → `status` (4값 통일: 접수중/마감/상시/확인필요)
- 원본 보존 이유 3가지:
  1. 추론 함수 한계 시 디버깅 추적
  2. 행정 신뢰성 — 사용자에게 원문 노출 옵션
  3. 통일 규칙 변경 시 과거 데이터 재분류 가능

### 왜 9일간 못 봤나 — 구조적 원인 5가지

1. **AI 의 시야 한계 — 작업 단위 (task scope)** — 그 순간의 한 파일/한 함수만 봄
2. **AI 책임 범위 — "지금 굴러가게" 까지만** — 미래·다른 환경은 명시 안 하면 안 봄
3. **교차 검증 한계 — 같은 사각지대 공유** — ChatGPT × Claude 비교는 인프라 빚 못 잡음
4. **Cursor 의 본질 — 파일 단위 편집기** — 환경 간 일관성 자동 점검 안 함
5. **기술 부채의 본질 — 침묵** — 다른 시스템 변경 (Actions 자동화 의도) 이 촉매가 될 때까지 안 보임

→ 솔로 + AI 협업 환경의 *구조적 산물*. 누구의 잘못도 아님.

### 향후 사각지대 줄이는 패턴 — 우선순위 3개

| # | 패턴 | 비용 | 효과 |
|---|---|---|---|
| 1 | **스키마 단일 출처** (`pipeline/_ensure_schema.py`) — 28+ 컬럼 한 곳 관리 | Phase 2-B 자연 발생 | 빚의 ~50% 구조적 차단 |
| 2 | **`docs/tech_debt.md` 빚 목록 파일** — 알게 된 빚 한 줄씩 기록 | 30초/추가 | 솔로 기억 한계 외부화 |
| 3 | **`.cursorrules` 환경 4개 영향 명시 패턴** — AI 작업 시 PC/운영/Actions/v2 강제 검토 | 한 줄 추가 | 9일 사고 재발 차단 |

### 보조 패턴 (시간 들어 천천히)

- AI 작업 시 명시적 체크리스트 (스키마/connector/sync/merge/ALTER)
- "상태 표시 거짓말" 잡는 데이터 자체 모니터링 (Actions UI 무시, 운영 DB last_sync 직접 확인)
- 사이클 끝 영향 범위 회고 1줄
- AI 도구 역할 분리 (Claude = 시스템 추론 / Claude Code = 진단 / Cursor = 편집)

### 워크플로우 진화 — v1/v2 모드 분리

5/4 ~ 5/13 commit 패턴 자기 관찰: 사고 대응 / 인프라 정비 사이클은 거의 100% v1 직접. 원래 규칙 ("v2 → release → v1 cherry-pick") 이 사실상 안 지켜짐.

**원인 추정**:
- Cursor + Claude Code + 백업 + dry-run 이 *v2 검증 환경의 가치를 사실상 대체*
- 사이클 속도가 분 단위로 빨라져 v2 → release 마찰이 본 작업보다 큼
- v2 cherry-pick 적체 → 새 작업 v1 에서 → 적체 더 증가 (악순환)

**진화된 모드 분리** (의식적 채택):

| 모드 | 영역 | repo 흐름 | 안전망 | 해당 W20 작업 |
|---|---|---|---|---|
| **A** | 인프라 정비 / 사고 대응 | v1 직접 | 백업 + dry-run + 즉시 진단 | 세션 1 (b065) / 세션 2 (b064 kseafood) / 세션 3 (cherry-pick) |
| **B** | 새 기능 개발 | v2 → release/ → v1 cherry-pick | v2 환경 분리 + 단계적 검증 | 세션 4+ (AI 언어 통역 기능, 5월 말~) |

→ W20 세션 3 (v2 cherry-pick 통합) 이 *모드 B 부활을 위한 사전 정합 작업*. 적체 통째 해결 후 AI 언어 통역 시작 시 v2 깨끗한 base 에서 진입.

### 한 줄 결론

> *알게 된 빚 적어두기 + 스키마 한 곳에 모으기 + AI 에게 환경 차이 묻기 + 모드별 v1/v2 운영* — 이 4가지가 진화된 워크플로우. 백로그 065 Phase 2-B 가 첫 인프라 작업.

---

## 다음 사이클 (W20) — Phase 2-B 스코프 미리보기

진행 X. 사용자 결정 (1)~(4) 후 W20 fresh 사이클에서 진입.

### Phase 2-B 작업 단위 (5건)

1. **`pipeline/_ensure_schema.py` 신설** — 28+ 컬럼 통합 ALTER, idempotent
   - 23 컬럼 (appy.py:319-346 ported) + 5 누락 컬럼 (`synced_to_render`, `synced_at`, `notice_chk`, `notice_order`, `notice_create_dt`) + 향후 추가 컬럼
2. **connector 측 import + 호출** — at_global / kseafood / jbexport_daily / update_db
3. **appy.py:319-346 consolidation** (선택, 별 사이클 가능)
4. **yaml 변경**:
   - install deps 다음 schema ensure step 추가 (c-light)
   - jbexport step 다음 `/api/sync` step 추가
   - 마지막 aggregation step (exit code 1 if `batches_fail > 0`)
   - timeout 60 → 75분 상향
   - `continue-on-error` 정책 재검토 (현 전수 적용은 fix 대상)
5. **Secrets 추가** (사용자 작업) — ADMIN_KEY 1개

### 추정 시간

W20 사이클 — **3~4시간** (어제 추정 2시간의 ~2배). 진단 시 발견된 schema fragmentation + aggregation 설계가 추정 확대 원인.

### 부수 백로그 검토

- 5c Kakao 항구 실패 (KAKAO_REFRESH_TOKEN 부재) — 별건 백로그 신설 후보
- 콘솔 cp949 mojibake — 별건 후보
- at_global / jbtp_related 사이트 측 삭제 row 처리 — DB sync_status 정책 별건

→ 모두 W20 이후 사이클 후보. 5/13 본 사이클에서는 등록 X (진단 only).

---

## 5/13 종료

자동화 검증 1차 PASS + 백로그 065 Phase 2-A 진단 종합 종료. 사이클 분리 완벽 (사고 0, fix 0, commit 0). W20 fresh 사이클 진입 대기.
