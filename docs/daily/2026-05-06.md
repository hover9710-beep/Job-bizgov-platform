# 2026-05-06 작업 일지

## 메타
- 작업 시간: 형식님 주말/저녁
- 영향 repo: v1 운영, v2 dev (양쪽)
- 핵심 commit: v1 `788e54a` (운영 배포), v2 `e944186` (backport)
- 부수 commit: `.cursorrules` 양쪽 (v1 `695f0ca`, v2 `8074ca9`)
- 운영 사고: 없음

## 작업 요약
"오늘 가장 많이 본 공고" 위젯이 필터(site/source 등) 적용 시 항목이 바뀌거나 사라지는 UX 버그를 운영 환경에서 수정. 동시에 숨어있던 SQL ambiguous column 버그도 발견하여 함께 수정.

## 단계별 진행

1. v2 dev에서 패치 작성 (`da56962`)
   - get_today_top_projects → get_today_top_clicked 함수 교체
   - 두 라우트(/와 /new)의 18줄 top_clicked 블록을 1줄로 축약
   - 로컬 4시나리오 검증

2. v1 cherry-pick 시도 → 충돌
   - 원인: v1↔v2가 우리 패치 외에도 7곳에서 갈라져 있음 (load_latest_*, check_admin, /recommend, /run, latest_*)
   - 결정: surgical 수동 패치로 우회

3. v1에 surgical 패치 적용
   - 백업: appy.py.backup_2026-05-06
   - Cursor로 3곳만 정밀 수정 (line 837, 1390, 2596)
   - Select-String 3개로 검증 통과

4. 로컬 검증 중 위젯 안 뜨는 문제 발견
   - /new는 표시, /는 미표시 → 처음엔 라우트별 버그 의심
   - 실제 원인: 로컬 click_log 0건 → 위젯 조건부 숨김 (정상 동작)
   - 테스트 데이터 6건 삽입 후 → /new는 정상, /는 여전히 미표시

5. 진짜 원인 발견 (Cursor 진단)
   - SQL 에러: `OperationalError: ambiguous column name: title`
   - GROUP BY의 alias `title`, `source`가 cl/bp 양쪽에 동시 존재
   - except로 빠져 폴백 함수에 의존 중이었음
   - 수정: GROUP BY에서 alias 대신 expression(COALESCE) 명시

6. 4시나리오 검증 통과 → 운영 푸시 (`788e54a`)
   - Render 자동 배포
   - 라이브 4 URL 동일 항목 확인

7. 정책 강화 + 정합성 후속 작업
   - `.cursorrules` 양쪽 repo에 추가 (Cursor 자동 git 실행 차단)
   - v2에 SQL fix backport (`e944186`)

## 발견 및 교훈

### v1 ↔ v2 divergence 7가지 (다음 동기화 작업 참고)
1. load_latest_by_source 함수 (v2에만)
2. load_latest_misc 함수 (v2에만)
3. check_admin(req) → check_admin(request) + form 체크 (v2에만)
4. /recommend에 is_admin 전달 (v2에만, 3곳)
5. /run admin 체크 시작부 추가 (v2에만)
6. latest_* 템플릿 변수 5개 (v2에만)
7. 오늘 패치한 top_clicked 영역 (양쪽 동기화 완료)

### 패턴: alias-vs-expression GROUP BY
LEFT JOIN 후 SELECT에서 동일 alias로 다른 테이블 컬럼을 통합할 때, GROUP BY에서 alias 사용 시 SQLite는 ambiguity 판정 가능. expression 그대로 쓰는 게 안전.

### 패턴: try/except 폴백의 위험
get_today_top_clicked는 SQL 에러를 except로 잡아 []만 반환 → 호출자는 폴백 함수로 넘어가 정상처럼 보임. **사용자에게는 정상 보이지만 의도된 코드는 작동 안 함**. except 블록에 print만 있으면 production 로그에 기록되어도 알아채기 어려움. 향후 추가 함수 작성 시 except에서 더 명시적인 에러 신호 필요.

### 도구 정책 위반 발생
Cursor가 사용자 동의 없이 git commit + push를 자동 실행. 메모리 정책("AI는 git commit/push 금지")에 어긋남. 즉시 `.cursorrules` 추가로 차단 명시.

## 운영 영향
- 다운타임: 0
- 사용자 영향: 위젯 동작 개선만 (역방향 영향 없음)
- 롤백 지점: `bc1c44b85157a540356c1792cff87681562af245`

## 다음 사이클 작업
- [ ] B 문서화 (이 작업)
- [ ] C 로컬/repo 정리: `appy.py.backup_2026-05-06`, `_tmp_*`, `v1_v2_diff.txt` 정리, `.gitignore` 보강
- [ ] (선택) v1 ↔ v2 divergence 7개 항목 일괄 동기화 계획 수립

