# 2026-05-17 일지 — W21 b069 Phase 2 AI 한줄요약 완비 + 시뮬 누적 시스템 정착

## 한 줄 요약

Phase 2 (`ai_summary`) **완료** — widget 노출 대상 `start_date >= 2026-01-01` 의 ai_summary coverage **100% (2,901/2,901)**, 전체 18.0% → 64.6% (+46.6%p). backfill (`--widget-targets`) + `synced_to_render` 수동 reset + `/api/sync` 실행 (2,121 row, errors=0). 비용 ~$0.10. **시뮬 누적 시스템** 정식 정착 — `docs/simulations/INDEX.md` 신설, Phase 2 entry 가 1번째 정식 누적. **사후 발견**: cache UPDATE 가 sync flag reset 안 함 → Phase 2.1 patch 백로그.

---

## 사이클 개요

### 사전 (시뮬)

- 본 사이클 시작 시점에 widget 미생성 = 1,641 추정 (raw SQL count)
- 시뮬 entry `docs/simulations/2026-05-17_phase2_ai_summary.md` 작성 (9단계 + 5보완)
- `docs/simulations/INDEX.md` 신설 (3번째 entry 가 첫 정식 누적)
- 사용자 결정 6건 명세 → 옵션 C (5/17 EOD 즉시) 채택

### 실행 (Step 7~10)

| Step | 시간 | 결과 |
|---|---|---|
| 7. backfill | 1분 | pending=1, generated=1, failed=0 (선행 cron 이 13:26~13:45 사이 1,640건 이미 처리) |
| 8. sync | 2분 | reset 2,121 → POST /api/sync → inserted=1, updated=2,120, errors=0 |
| 9. 검증 | 1분 | widget 100%, 전체 64.6%, sync pending=0, Render 200 OK |
| 10. commits | — | 5건 (시뮬 / 백로그 / release / daily / LATEST) |

### 사후 (회고)

- 시뮬 정확도: widget 처리 대상 1,641 추정 → 실측 누적 2,121 (선행 cron 동시 작동 미예측)
- 비용 추정 $0.09 → 실측 ~$0.10 (정확도 90%)
- 시뮬 미발견 1건: `ai_summary_cache.py` UPDATE 가 `synced_to_render` reset 안 함

---

## 핵심 사고 — sync flag reset 누락

### 현상

`pipeline/ai_summary_cache.py` 의 UPDATE 문:

```python
UPDATE biz_projects
SET ai_summary = ?, ai_summary_at = ?, updated_at = CURRENT_TIMESTAMP
WHERE id = ?
```

→ `synced_to_render` 는 그대로 (대부분 = 1). sync 가 push 할 row 0.

### 우회 (본 사이클)

수동 SQL reset:

```sql
UPDATE biz_projects
SET synced_to_render = 0
WHERE ai_summary_at >= '2026-05-17'
  AND ai_summary IS NOT NULL AND ai_summary != ''
```

2,121 row reset → sync 후 운영 정상 반영.

### 진짜 해결 (Phase 2.1, 다음 사이클)

- `ai_summary_cache.py` UPDATE 에 `synced_to_render = 0` 컬럼 추가
- 같은 패턴: `ai_translate_cache.py` (b066) 도 확인 필요
- 영향 범위 검증: 다른 backfill 모듈 (`ai_friendly_*` 등) 동일 누락?

---

## 시뮬 누적 시스템 정착 — 의미

### 사전 (5/17 이전)

- 시뮬 자체는 b066 (Phase 2-Alpha) 부터 본격 적용
- 단 결과물이 conversational 상태로만 존재, 파일로 미persist
- "선행 entry 회고" 가 다음 사이클 시작 시 손실됨

### 본 사이클 (5/17 EOD)

- `docs/simulations/` 디렉터리 신설
- `2026-05-17_phase2_ai_summary.md` = 첫 정식 entry
- `INDEX.md` = 시간순 누적 + 정확도 추세 추적
- 회고 미작성 entry 2건 (b066, b033) backfill 권장으로 명시

### 다음 사이클부터

1. 새 기능 진입 → `docs/templates/feature_impact_simulation.md` 적용
2. `docs/simulations/INDEX.md` 의 선행 entry 정확도 / 함정 학습
3. 신규 entry 를 `YYYY-MM-DD_<백로그>.md` 로 저장
4. INDEX 에 1줄 추가
5. 진행 후 회고 섹션 채움 → 정확도 누적

---

## 5/20 시연 영향

- ai_summary coverage widget = **100%** → 위젯 상세 페이지 한줄 요약 누락 0
- 차별점 (gpt-4o-mini 자동 통역 + 한줄 요약) 시연 가능
- 운영 사이트 (https://job-bizgov-platform.onrender.com) 즉시 반영됨

---

## 다음 우선순위 (5/18~)

🔴 1. **5월 20일 공모 응모서** (D-3, 풀타임)
🟡 2. **Phase 2.1 patch** — `ai_summary_cache.py` + `ai_translate_cache.py` 의 `synced_to_render = 0` 누락 fix (30분, 다음 사이클)
🟢 3. cron 모니터링 (5/18 새벽 KST 06:00 자동 실행 확인)
🟢 4. Phase 3 (첨부 분석) 시뮬 작성 (시연 후, W21 후반)

---

## commits (5건)

1. `docs(simul-system)`: 시뮬 누적 시스템 정착 — Phase 2 entry + INDEX 신설
2. `docs(b069)`: Phase 2 AI 한줄요약 백로그 — backfill + sync + Actions step 사후 문서
3. `docs(release)`: Phase 2 release entry — backfill + sync 결과 (2026-05-17 EOD)
4. `docs(daily)`: 2026-05-17.md — Phase 2 완비 + 시뮬 시스템 정착
5. `docs(LATEST)`: 진입점 갱신 — Phase 2 완료, Phase 2.1 patch 우선

---

## 배경 / 메모

- 선행 cron 작동 (13:26~13:45) 확인 — daily-crawl.yml 의 b069 step (`--widget-targets --limit 200`) 이 실제 운영. 단 200/일 throttle 로 trickle. 본 사이클의 즉시 backfill 로 시연 시점 안전 도달.
- 백업: `db/biz.backup.20260517_224529_pre_phase2.db` (SHA256 매칭 PASS)
- 운영 ROLLBACK 가능 (< 5분)
- b033 dedup 시너지: bizinfo 처리 대상 5배 감소 → backfill 비용 1/5
