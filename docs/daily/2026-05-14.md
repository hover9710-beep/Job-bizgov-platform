# 2026-05-14 일지 — W20 백로그 065 Phase 2 (Pre/A/B) 코드 작성 + dry-run trigger 대기

## 한 줄 요약

5/13 daily 의 사용자 결정 4건 (raw_status 처리=`pipeline/_ensure_schema.py 신설 b+c-light` / Actions DB=`매 run 임시` / continue-on-error=`핵심 step 만 제거, 외부 의존 유지` / 첫 trigger=`dry-run + Secrets 는 Phase 2-B 직전`) 확정 후 모드 A (인프라 정비, v1 직접) 로 Phase 2-Pre/A/B 코드 작성 완료. **사고 0, 사이클 분리 정책 준수**. Phase 2-Pre: `pipeline/_ensure_schema.py` 신설 (36 컬럼 통합 idempotent ALTER, 4 테이블) + 4 import (at_global / kseafood / jbexport_daily / update_db). v1 로컬 DB 멱등 검증 PASS (added=0, SHA256 변동 0), Actions runner 빈 DB 시뮬 PASS (added=34). Phase 2-A: `.github/workflows/daily-crawl.yml` 의 8 crawler step `id:` 추가 + summary step 전체 재작성 (`if: always()` + outcome 검사 + master 실패 시 exit 1, aux 실패는 WARN). Phase 2-B: schema ensure step 신설 (c-light) + `/api/sync --dry-run` step 신설 (`secrets.ADMIN_KEY`) + summary 에 ensure-schema/sync outcome 포함 + critical 판정 + timeout 60→75분. workflow_dispatch 첫 dry-run trigger 결과 대기 — 사용자 commit/push/Secret 추가/trigger 단계 수동 진행.

---

## 사용자 결정 사항 4건 (5/13 daily ☐ → 5/14 ✅)

| # | 항목 | 결정 |
|---:|---|---|
| (1) | raw_status 스키마 처리 | **`pipeline/_ensure_schema.py` 신설 (b+c-light)** — 28+ 컬럼 통합 idempotent ALTER 모듈, connector import + yaml init step 양쪽 호출 |
| (2) | Actions runner DB 패턴 | **매 run 임시 DB** — 운영 DB single source-of-truth, `/api/sync` 로만 갱신, url UNIQUE 가 멱등 처리 |
| (3) | continue-on-error 정책 | **핵심 step 만 제거, 외부 의존 유지** — crawler/summary critical 판정 = false-effective (외부 사이트 일시 장애 흡수) |
| (4) | workflow_dispatch 첫 실험 + ADMIN_KEY Secrets | **dry-run 먼저 + Secrets 는 Phase 2-B 직전 추가** — Phase 2-Pre + 2-A 완료 후 dry-run trigger → Secret 검증 후 본 실행 |

---

## Phase 2-Pre — pipeline/_ensure_schema.py 신설 + 4 import

### 새 모듈 — `pipeline/_ensure_schema.py` (+158줄)

설계:
- 36 컬럼 통합, 4 테이블 (`biz_projects` 29 / `companies` 5 / `visit_log` 1 / `click_log` 1)
- 출처: `appy.py:121-156` (line-by-line ensure) + `appy.py:319-346` (`_safe_add` 리스트)
- API: `ensure_schema(conn)` / `ensure_schema_path(db_path)` / `main()` CLI
- 멱등 — `PRAGMA table_info` 검사 후 부재 시만 ALTER
- 안전 — 테이블 자체 부재 시 `skip` (Actions runner 빈 DB 초기 상태 호환)
- duplicate column race-condition 흡수

### 4 import + 호출 추가

| 파일 | 진입점 | +라인 |
|---|---|---:|
| `connectors/connector_at_global.py` | `save_to_db()` 시작 | +2 |
| `connectors/connector_kseafood.py` | `save_to_db()` 시작 | +2 |
| `pipeline/jbexport_daily.py` | `sync_status_to_db()` 의 conn open 직후 (raw_status UPDATE 실패 지점) | +2 |
| `pipeline/update_db.py` | `_init_db()` 진입부 (run_all 의 4d step 경로) | +2 |

### 검증

| 항목 | 결과 |
|---|---|
| v1 로컬 DB 백업 (SHA256 매칭) | `db/biz.db.backup_b065_phase2pre_20260514_223148` ✅ |
| v1 로컬 DB CLI 1회 | `added=0 skipped_existing=36` (멱등 PASS) |
| v1 로컬 DB SHA256 변동 | `83eb013dd2b5463f` — backup 과 동일 (변동 0) ✅ |
| 빈 DB 시뮬 (Actions runner) | `added=34` (raw_status / synced_to_render 포함 전부) ✅ |
| visit_log/click_log 없음 | `skipped_no_table=2` (safe skip) |
| 4 module import | 순환 없음, traceback 0 |
| 멱등 재실행 | `added=0` 유지 |

---

## Phase 2-A — daily-crawl.yml continue-on-error 정합화 (+58 -1)

### 변경

| 항목 | 변경 |
|---|---|
| 8 crawler step | `id:` 추가 (`crawl-bizinfo` / `crawl-kstartup` / `crawl-at-global` / `crawl-kseafood` / `crawl-jbbi` / `crawl-jbtp` / `crawl-jbtp-related` / `crawl-jbexport`). `continue-on-error: true` 유지 (외부 의존 흡수) |
| summary step | 전체 재작성 — `if: always()` + 8 step outcome 검사. master (bizinfo / jbexport) 실패 시 exit 1 → workflow UI 정확히 failure 표시. aux 6 connector 실패는 WARN 만 |

### 동작 시나리오

| 시나리오 | 결과 |
|---|---|
| 정상 (전부 success) | summary `EXIT 1` X → workflow UI **success** ✅ |
| bizinfo 실패 | summary `CRITICAL: bizinfo` + `EXIT 1` → workflow UI **failure** |
| jbexport 실패 (raw_status 같은 사고 지점) | 동일 → workflow UI **failure** |
| kseafood 만 timeout (aux, 외부 사이트 일시) | summary `WARN: aux ... kseafood` → workflow UI **success** (외부 의존 흡수) |
| 2개 이상 aux 실패 | 동일 WARN 만 |

### 5/13 daily 진단 8건 대응

| # | 항목 | Phase 2-A 처리 |
|---|---|---|
| 1 | continue-on-error 전수 → UI Success 거짓 | ✅ master 실패 시 summary exit 1 로 차단 |
| 4 | /api/sync POST step 0개 | Phase 2-B |
| 8 | timeout 합계 60분 (여유 0) | Phase 2-B |

---

## Phase 2-B — yaml /api/sync step + schema ensure step + critical 정합 (+104 -4)

### 변경 (총 4건)

| # | 항목 | 변경 |
|---|---|---|
| 1 | job timeout | `60` → `75` 분 (진단 #8 대응) |
| 2 | "Ensure DB file + schema" step | **신설** (Install deps 직후, id=`ensure-schema`, timeout 2분). `python pipeline/_ensure_schema.py` 호출 — c-light 방어 깊이. continue-on-error 미설정 (default false → 실패 시 정합 차단) |
| 3 | "Sync to Render (dry-run)" step | **신설** (jbexport 직후, id=`sync-to-render`, `if: always()`, timeout 10분, `continue-on-error: true`). env `ADMIN_KEY: ${{ secrets.ADMIN_KEY }}`. `python pipeline/sync_to_render.py --dry-run` |
| 4 | summary step | `O_ENSURE` + `O_SYNC` outcome env 추가. critical 판정에 ensure-schema 실패 / sync-to-render 실패 추가 (ADMIN_KEY 부재 시 명시 메시지) |

### Step 흐름 (14 step)

```
Checkout → Setup Python → Install deps → Ensure DB schema (c-light)
  → 8 crawler (bizinfo / kstartup / at_global / kseafood / jbbi / jbtp / jbtp_related / jbexport)
  → Sync to Render (dry-run)
  → Crawl summary + status aggregation
```

### Critical 판정 (summary exit 1)

- ensure-schema 실패
- bizinfo / jbexport 실패 (master crawler)
- sync-to-render 실패 (ADMIN_KEY 부재 또는 /api/sync 오류)

### 정합성 검증 (PyYAML 미설치 → regex 구조 검사)

| 항목 | 결과 |
|---|---|
| 총 step 수 | 14 (+2 from Phase 2-A) ✅ |
| step id 수 | 10 (8 crawler + ensure-schema + sync-to-render) ✅ |
| `continue-on-error: true` | 9 (8 crawler + sync) ✅ |
| `if: always()` | 2 (sync + summary) ✅ |
| timeout-minutes (job) | 75 ✅ |

> ⚠️ PyYAML 미설치 환경 — full parse 검증은 GitHub Actions 측 dispatch 시점에 확정.

---

## 변경 파일 종합 (Phase 2-Pre + 2-A + 2-B)

| 파일 | 분류 | 라인 |
|---|---|---:|
| `pipeline/_ensure_schema.py` | **신설** | +158 |
| `connectors/connector_at_global.py` | import + 1줄 호출 | +2 |
| `connectors/connector_kseafood.py` | 동일 | +2 |
| `pipeline/jbexport_daily.py` | 동일 (sync_status_to_db 진입부) | +2 |
| `pipeline/update_db.py` | 동일 (_init_db 진입부) | +2 |
| `.github/workflows/daily-crawl.yml` | id 추가 + 2 step 신설 + summary 재작성 + timeout 60→75 | +104 -4 |

**총 6 파일, +270 -4**.

---

## 🟨 사용자 단계 (5/14 종료 시점 대기 중)

### Step 1: commit (v1, push 보류)

```bash
git add pipeline/_ensure_schema.py \
        connectors/connector_at_global.py \
        connectors/connector_kseafood.py \
        pipeline/jbexport_daily.py \
        pipeline/update_db.py \
        .github/workflows/daily-crawl.yml
git commit -m "feat(b065 phase 2): pipeline/_ensure_schema.py + daily-crawl.yml 정합화 + /api/sync dry-run step"
```

### Step 2: GitHub Secrets — ADMIN_KEY 추가

`Settings → Secrets and variables → Actions → New repository secret`
- Name: `ADMIN_KEY`
- Value: Render Dashboard 의 `ADMIN_KEY` 환경변수와 **동일 값**

### Step 3: push + workflow_dispatch dry-run

```bash
git push origin main
```

URL: `https://github.com/hover9710-beep/Job-bizgov-platform/actions/workflows/daily-crawl.yml` → **Run workflow** (main)

### Step 4: 결과 분석 후 의사결정 (W20 다음 사이클)

검증 PASS 시 — `--dry-run` 제거 + commit + push + 본 실행 + 운영 위젯 검증.

---

## 모드 A (인프라 정비, v1 직접) 준수 자기 평가

5/13 회고에서 결정한 모드 분리 ([feedback_cycle_separation.md] 정합):

| 모드 A 조건 | 5/14 준수 여부 |
|---|---|
| 영역: 인프라 정비 / 사고 대응 | ✅ Actions 자동화 인프라 (5/3 disable 대체) |
| repo 흐름: v1 직접 | ✅ v2 우회 없음 |
| 안전망: 백업 + dry-run + 즉시 진단 | ✅ DB 백업 SHA256 매칭 + Actions dry-run + Phase 단위 검증 |
| 사이클 분리: 사고 발견 시 즉시 멈춤 | (사고 0건, 적용 X) |

### 사이클 분리 메타

| 지표 | 5/12 마라톤 | 5/13 진단 | **5/14** |
|---|---:|---:|---:|
| 사고 발생 | 5건 | 0건 | **0건** |
| inline fix | (사이클 내) | 0건 | **0건** |
| code commit | 5+ | 0 | **(예정 1, 사용자 결정 대기)** |
| Phase 분리 | (단편 fix 5+) | (진단 only) | **Pre/A/B 3 phase 명확 분리** ✅ |

---

## 미해결 / 이월

| # | 항목 | 처리 시점 |
|---|---|---|
| (a) | workflow_dispatch dry-run 첫 결과 분석 | 사용자 trigger 후 즉시 |
| (b) | --dry-run 제거 + 본 실행 | (a) PASS 후 W20 다음 사이클 |
| (c) | v2 cherry-pick (b065 Phase 2 코드) | (b) PASS 후 묶어 진행 (모드 A 안전망 — v2 깨끗한 base 유지) |
| (d) | 운영 DB ensure_schema 실행 (사용자 Render Shell) | (b) 본 실행 직전 1회 — `python3 pipeline/_ensure_schema.py --db /var/data/biz.db` |
| (e) | PC daily run 의 sync step (b057 Phase 2.1f wrapper) 활성화 | Actions 자동화 검증 PASS 후 별도 판단 — 보험으로 유지 또는 비활성 |
| (f) | 백로그 058 (v1 connector 정지 진단) 의미 재정의 | Actions 자동화 정착 후 — PC 가 실험용이면 058 자체 무의미 가능 |

---

## 5/14 종료 (사용자 trigger 대기)

Phase 2-Pre + 2-A + 2-B 코드 작성 종합 종료. workflow_dispatch dry-run trigger 결과 입수 후 다음 사이클 (W20 진행 또는 W21 진입) 결정.

**모드 A 인프라 정비 완전 준수 — 사고 0, fix 0, code 변경 6 파일 (Pre/A/B 단위 명확 분리), 사용자 결정 4건 모두 반영.**
